# CONTEXT.md — Working Handoff

**Project:** wind-tunnel — a SaaS pricing & packaging studio (product name **Wind Tunnel**; see §11)
**Doc role:** §0 is the concise handoff and current source of truth. The remaining sections preserve the detailed product contract, math specification, phased plan, owner decisions, and historical log. The public-facing story lives in README.md.
**Last committed version:** v0.17.4 · **Current status:** the §6 build plan is code-complete and the pre-release audit series is committed (P0 through P9-partial and every v1.1 extension including P7e). v1.0 release remains gated only by owner-driven items: P0 remote CI + Vercel deployment, and P9's manual accessibility matrix, owner user-test, and README screenshots. · **Last verified:** 2026-07-27 (S34; timestamps are owner-directed)

---

## 0. Current handoff (read first)

### 0.1 Verified repository state

| Fact | Verified state |
|---|---|
| Version | Latest committed release: **v0.17.4** — `Commit v0.17.4: Spec Coverage + Release Handoff /5-F5-T6`, post-dated 2026-07-27 01:15 +05:30. The completed audit series is `v0.17.0` Shell Recovery + Model Conventions (`/1-F5-T6`, 00:15), `v0.17.1` Research Import Safety + Limits (`/2-F5-T6`, 00:30), `v0.17.2` Linter + Positioning Integrity (`/3-F5-T6`, 00:45), `v0.17.3` Share + Decision Record Clarity (`/4-F5-T6`, 01:00), and v0.17.4. `package.json`, both lockfile root versions, the footer, and README's Version Control block agree at v0.17.4. |
| Git | `main` tracks `origin/main`. No tags, other branches, or submodules. |
| Working tree | Clean after the v0.17.0–v0.17.4 audit series. Every §6 phase and all audited fixes are committed. |
| Product | Every workbench is live: Model, Design, Simulate (KPIs, dots, waterfall, sweeps, mechanism view, A/B compare), Analyze — Uncertainty (Monte Carlo, tornado), Analyze — Research (PSM, Bundling, Conjoint, MaxDiff with pooled bridge), Analyze — Positioning (competitor entry, segment-scoped Pareto map, break-even rays, direct-dominance verdicts, per-segment competitor share, live competitor-loss KPI), and Share (Decision Record, Pricing-page mock, Scenario transfer). Exact own/competitor identity ties are competitor-first. |
| Remote/deploy | GitHub remote is `https://github.com/ItsMonarch04/wind-tunnel.git`. Vercel is selected but not connected or deployed. |

### 0.2 Plain-language current state

**The §6 build plan and audited pre-release fixes are code-complete.** Every phase (P0–P9-partial and every v1.1 extension including P7e) has landed, and the completed v0.17.0–v0.17.4 series resolves B-010…B-025. The v1.0 release is gated only by owner-driven items: P0's remote CI + Vercel deployment, and P9's manual accessibility matrix + owner user-test + README screenshots. Local checks at v0.17.4: **133 unit tests**, tri-browser E2E **44 passed / 4 skipped / 0 failed**, typecheck, ESLint, Prettier, spec coverage (§4.1–§4.12 all required), privacy scan, static build, **310.9 KiB / 320 KiB** gzip client-JS.

### 0.3 Next work, in priority order

No implementable §6 phase remains; every next item below is owner-driven. The audited pre-release fixes are complete and their source report has been removed.

1. **Owner — P0 Vercel connection.** Connect the GitHub repository to Vercel so P0 can pass its deployed-shell acceptance check. `vercel.json` and static host headers are ready.
2. **Owner — P0 CI gate.** Open or authorize a test PR so GitHub Actions runs its defined workflow (typecheck, lint, format, test, static build + bundle, Chromium audit).
3. **Owner — P9 manual gates for v1.0.0.** Assistive-technology matrix (VoiceOver/NVDA, 200% zoom, forced colors, screen-reader path); URL → decision-record ≤ 15-minute user test; README screenshots; MODEL-SPEC final read-through against implementation.

### 0.4 Verified defects/backlog (not optional product ideas)

| ID | Status | Evidence and disposition |
|---|---|---|
| B-001 | **Fixed (owner decision S10; committed)** | Exact own/competitor `(value, price)` ties are competitor-first, attributing the mass to `CompetitorLoss` rather than own revenue. T-CMP-08 verifies attribution and input-order independence. |
| B-002 | **Fixed (committed in v0.0.4)** | MaxDiff's all-equal branch divided 100 by the per-task display count instead of total items and did not guard unseen items; §4.10 now uses total scored items and rejects zero-appearance inputs. |
| B-003 | **Fixed (committed in v0.0.4)** | Base assumptions, uncertainty bands, zero-width bands, and P4 inputs conflicted; §§4.1, 4.8, and P4 now share one P50-centered, positive-domain contract. |
| B-004 | **Fixed (committed in v0.0.4)** | T-CMP-05 asserted a false constant-participation delta; it now checks only valid share/conservation behavior. |
| B-005 | **Fixed (committed in v0.0.4)** | The pooled CBC model could not produce respondent-specific segment part-worths; §4.10 now defines an aggregate proposal explicitly applied to selected segments. |
| B-006 | **Fixed (committed in v0.0.4)** | Fixed price-sweep bounds could miss the revenue peak at high spread; §4.4 now expands the search and always evaluates the current price. |
| B-007 | **Fixed (committed in v0.0.4)** | Competitor synthesis had two module owners, the derivative gate used brittle dual tolerances, exact random balance was sometimes impossible, and the release order omitted a dependency. Module/phase/test contracts are now singular and consistent. |
| B-008 | **Fixed (committed in v0.0.4)** | README described planned tests, WCAG audit, and Vercel deployment as completed, and its roadmap mixed v1.0 with v1.1. It now uses future tense and separates releases. |
| B-009 | **Fixed (owner decision S10; committed)** | T-STAT-02 requires `1e-9` on its interior grid and the owner accepted `1e-8` at the `±6σ` endpoints, where binary64 rounding produces a measured `8.85e-9` error at +6σ. |
| B-010 | **Fixed (v0.17 audit series; committed)** | Any non-share URL hash (e.g. the skip link's `#workbench-panel`) blocked localStorage restore, showed a share-link error, and let autosave overwrite the stored scenario from a blank state; the shell now decodes only `#s=` hashes and always restores autosave otherwise. |
| B-011 | **Fixed (v0.17 audit series; committed)** | Cancelling the Positioning "apply one overall value" prompt coerced `null` to `0` and overwrote every per-segment competitor value; cancel and empty input are now no-ops. |
| B-012 | **Fixed (v0.17 audit series; committed)** | Linter E4 measured downgrade mass against add-on composites instead of the value-maximal tier (§4.9), producing false "Large downgrade mass" warnings in menus with add-ons; the maximum is now taken over bare tiers and a fixture pins it. |
| B-013 | **Fixed (v0.17 audit series; committed)** | The theme toggle's cycle made the explicit `dark` setting unreachable on dark-OS machines; it now cycles system → dark → light. |
| B-014 | **Fixed (v0.17 audit series; committed)** | σ presets/range shipped as narrow/moderate/wide 0.2/0.4/0.6 with an unbounded custom input, contradicting MODEL-SPEC §4.12 (Low 0.25 / Medium 0.5 / High 0.9 on [0.05, 2.0]); the UI now matches §4.12, the schema caps σ at 2, and §4.12 is a required spec-coverage section. |
| B-015 | **Fixed (v0.17 audit series; committed)** | Imported research records were never cross-validated, and the throwing estimators ran unguarded in render, so a corrupt import crashed the Research tab persistently via autosave; the schemas now fail closed, every estimator call site is guarded, and an app-level error boundary catches the rest. |
| B-016 | **Fixed (v0.17 audit series; committed)** | The Model KPI rail re-implemented the state-to-engine adapter inline; it now consumes the shared `simulateScenarioDesign` path. |
| B-017 | **Fixed (v0.17 audit series; committed)** | Duplicate feature IDs inside a fence passed the schema; the envelope engine dedupes but the positioning map summed raw IDs, plotting a different tier value than the simulation used. The schema now rejects repeated fence features. |
| B-018 | **Fixed (v0.17 audit series; committed)** | Removing a research study stored the key as explicitly `undefined`, which Zod preserves, so the Share surface counted a ghost "excluded research artifact"; removal now deletes the key. |
| B-019 | **Fixed (v0.17 audit series; committed)** | The WTP density strip printed its display clamp ("σ 0.08") instead of the user's actual σ. |
| B-020 | **Fixed (v0.17 audit series; committed)** | The engine-purity ESLint rule missed `../../lib/state/...` specifiers and dynamic `import()`; both are now covered and probe-verified. |
| B-021 | **Fixed (v0.17 audit series; committed)** | Linter E2 flagged a tier as dominated using only its bare-tier share, so a tier selling entirely through an add-on composite was reported as inert dead weight; E2 now aggregates the tier and all composites built on it. |
| B-022 | **Fixed (v0.17 audit series; committed)** | The scenario had no rename control anywhere, so every Pricing Decision Record shipped with a template or placeholder title; Share → Scenario transfer now owns the name. |
| B-023 | **Fixed (v0.17 audit series; committed)** | Dark-OS users saw a light-theme flash on every load; a static pre-hydration bootstrap now applies the persisted theme before paint. |
| B-024 | **Fixed (v0.17 audit series; committed)** | The Decision Record had no Positioning section despite §6 P8 specifying one; it now reports per-segment competitor share and direct-dominance verdicts when competitors are active. |
| B-025 | **Fixed (v0.17 audit series; committed)** | Competitors off the Pareto frontier disappeared from the map with no disclosure; a note now names them and states that they still compete in the simulation. |

All B-series defects above are fixed and committed. Per-phase invariants worth preserving across future work: P3's share payload excludes research artifacts by construction; P5's linter only receives an injected, deterministic menu re-simulation path; P6a's Simulate reuses the P5 state-to-engine adapter (single source of truth); P7a's Monte Carlo samples only size/WTP bands and shares each indexed draw vector across all designs (common random numbers); P7b's PSM excludes rather than rewrites monotonicity violators; P7d-2's Conjoint bridge only opens when the pooled price coefficient's one-sided 90% upper bound stays strictly negative; P7e's Positioning reuses P1's competitor-offer synthesis in `lib/engine/offers.ts` — competitors contribute to `CompetitorLoss`, never to `OwnBuyerSurplus`.

### 0.5 Optional product ideas (not approved; safe to defer unless noted)

- **Visual/interaction:** change-to-consequence highlighting, explicit save/undo confidence, denser workbench hierarchy, and board-ready record styling.
- **UX/conversion:** a short outcome-led worked-example path, decision-readiness summary, model-limit disclosures, and an exact share-payload preview. Trust disclosures and share preview should land with their core surfaces; richer guidance can defer.
- **Performance/accessibility:** worst-case input-to-paint profiling, calm live-region behavior, an expanded assistive-tech/device matrix beyond the required P9 baseline, and cold-cache Web Vitals gates. Required verification must not defer if public claims are made.
- **Content/operations:** privacy/data-lifecycle documentation, migration/recovery controls, a Vercel release/rollback runbook, and privacy-compatible moderated launch testing. Core data safety and the runbook should not defer past public launch.

### 0.6 Agent protocol and non-negotiables

1. Read §1–§3 for intent, §4 for normative math, §6 for the active phase, §7 for verification, and §8 for repository conventions. Architecture boundaries in §3.3 and the no-server/no-telemetry scope in §2.3 are non-negotiable unless the owner changes them.
2. Precedence on conflict: current owner decisions in §9 > normative body > README. Math truth is §4 until P1 extracts it to `docs/MODEL-SPEC.md`; MODEL-SPEC then becomes authoritative.
3. Every work session updates §§0.1–0.4 and appends a concise §13 entry. Completed work must move out of “in progress”; future work must never be described as shipped.
4. **Never commit or push unless the owner explicitly asks.** Before requesting approval: show the exact changed/staged files and summary; preserve and exclude unrelated changes; run formatting plus appropriate checks; update this handoff; align README, `package.json`, lockfile, footer, and version policy when those files exist; propose the exact version and message; then wait for explicit approval of both.
5. New runtime dependencies require a ledger entry and owner notification. Production dependency budget: ≤ 7 (§3.4).
6. A phase completes only when its §6 acceptance list and §7.5 protocol pass. Record failures honestly; never relabel incomplete work as complete.

---

## 1. Vision & core loop

**User & job.** A founder, PM, or pricing owner at an early-to-mid-stage SaaS is designing or revisiting packaging and wants a decision they can *defend* — before committing, without a consultant, and before any billing or CRM data exists.

**Thesis.** Packaging is usually decided by gut and competitor-copying, but Good/Better/Best is really a *screening mechanism* (second-degree price discrimination): a menu designed so that buyer segments with different willingness-to-pay sort themselves. If you make the buyer assumptions explicit — WTP distributions, how each segment values each feature — the menu's consequences stop being a matter of opinion. They can be computed.

**Core loop.** **Model** the buyers (segments, WTP distributions, segment × feature value matrix) → **Design** the packaging (tiers, fences, add-ons, free tier, price metric) → **Simulate** self-selection (each buyer picks the utility-maximizing offer; closed-form, instant) → **Reveal** the economics (revenue, conversion, capture rate, surplus kept by buyers, value lost to fencing and non-conversion, cannibalization) → tighten the assumptions that matter most (sensitivity tells you which) → repeat.

**Positioning.** Runs entirely on the user's assumptions, made explicit. Not a billing tool (Stripe/Paddle), not a data-driven optimizer that needs live revenue data. Useful before the first customer exists. Fully client-side: no accounts, no server, no analytics; data stays in the browser, scenarios share as URLs.

**The honest answer to "it's just your assumptions."** Yes — deliberately. The studio's stance: (1) you are already pricing on assumptions, just implicit ones; (2) the simulation shows what your assumptions *imply*, which is where the surprises live; (3) the sensitivity layer + assumption-provenance tags tell you which assumption drives the answer, so validation effort goes where it pays; (4) when you *do* run a survey, PSM and Conjoint/MaxDiff (§4.7, §4.10) fold their findings into the same model — assumption → evidence, in one loop. The output isn't "the right price" — it's a defensible decision plus a ranked list of what to validate first.

**What keeps this out of slop territory** (checkable claims, not vibes):
- One rigorous economic primitive powers everything (§4.2): the utility upper envelope over an offer set. Tiers, free tier, add-ons, bundling analysis, and competitor alternatives are all the same math — no per-feature hand-waving.
- Every formula in §4 has a named unit test against a closed-form or independently computed result (§7). A value-conservation identity holds to 1e-6 on every simulation, enforced by property tests.
- A "mechanism view" shows the actual screening picture — utility lines vs. buyer type, indifference breakpoints — for people who know the textbook figure. The economics is inspectable, not a black box.
- Behavioral effects (decoy, anchoring, choice overload) are handled as deterministic *linter checks with citations*, never as invented numeric uplift multipliers (§4.9, ledger D-08).
- Research methods are shipped *complete when released* — v1.0 includes PSM and sensitivity; v1.1 adds Conjoint/MNL, MaxDiff, and bundling under the contracts in §4. No shallow "conjoint-flavored" placeholders (ledger D-07).

---

## 2. Scope

### 2.1 v1.0 core + v1.1 extensions

| Surface | Contents |
|---|---|
| **Model** | Segments (2–6): size with P10/P90 band, seat count, median account-level WTP with P10/P90 band, within-segment spread, segment × feature value matrix (entered as total WTP × allocation shares, editable directly in advanced view), assumption provenance tags (guess / interview / survey / conjoint / benchmark + confidence) |
| **Design** | 1–5 tiers as feature fences over a shared catalog (≤ 12 features); flat or per-seat price metric; free tier (price 0); up to 3 add-ons; multiple designs per scenario for A/B |
| **Simulate** | v1.0: always-live closed-form self-selection per segment; KPIs (MRR, paid conversion, ARPA, capture rate); buyer-dot sorting; value waterfall; and per-tier price-sweep curves. v1.1: A/B compare with deltas/win-rate, mechanism view, and user-facing competitor-choice-share when Positioning is active. |
| **Analyze — Uncertainty** | Monte Carlo over assumption uncertainty (seeded, deterministic) with P10/P50/P90 and tornado chart, tornado × provenance → validation to-do list |
| **Analyze — Research** | v1.0 includes Van Westendorp PSM (manual/CSV survey input, validation, interpolated crossing points, optional illustrative mode clearly labeled). Reviewed v1.1 extensions add Choice-Based Conjoint (attributes/levels, balanced design generator, pooled MNL part-worth estimation, and an explicitly mapped aggregate WTP proposal), MaxDiff-lite importance scoring, and bundling economics (pure vs. mixed). Extension engines and views ship together in the order defined by §6; an unexposed engine is never marketed as available. |
| **Analyze — Positioning** | Competitor-as-alternative is implemented and tested in the v1.0 core engine/linter path, but is not marketed as a user surface by itself. The v1.1 Positioning extension adds competitor entry, loss-share readouts, and the segment-scoped map: Pareto points and break-even rays with a discrete dominance verdict (§4.11). |
| **Critic** | Design linter: economic checks (dominated offers, fence inversion, free-tier leakage with $ counterfactual, downgrade/cannibalization mass, dead fences, competitor loss) + behavioral checks (choice overload, weak anchor) — deterministic triggers, cited explainers |
| **Communicate** | Pricing Decision Record (assumptions + provenance, design, simulated economics, sensitivity, research findings when available, linter findings, alternatives considered) exported as Markdown and print-CSS PDF — this is required for v1.0. The pricing-page mock is a v1.1 extension after the record and core loop are green (D-06). |
| **Templates** | 3 archetype scenarios as data presets: PLG collaboration tool (per-seat + free tier), API/infra (flat tiers + add-on), sales-led B2B (2 paid tiers + enterprise) — solve the cold start, double as e2e fixtures |

### 2.2 Deferred/later backlog (§12) — with the cut rationale

- **Usage-based pricing engine.** Needs usage distributions per segment and price-as-function-of-consumption — a second engine. v1 models flat and per-seat metrics, where the screening insight lives. The value-metric question is still surfaced (Design field + Decision Record + linter guidance). (Ledger D-09.)
- **Trials & time dynamics** (retention, expansion, annual-vs-monthly terms). Temporal modeling is a different machine; v1 is a single-period screening model, stated plainly in the UI.
- **Joint price optimizer** ("suggest optimal prices"). The per-tier sweep + tornado gives the insight without over-claiming; a numerical joint optimizer invites "the tool said so" misuse. v1.1 candidate behind an explicit "local search, not truth" frame.
- **Hierarchical Bayes for Conjoint** (per-respondent part-worths). The first CBC release uses pooled MNL with pooled SEs — enough for an honest aggregate proposal; HB is a step-change in complexity that dilutes the correctness focus. (Ledger D-17.)
- **D-efficient design generation** for Conjoint. The first CBC release uses random-balanced designs (level counts differ by at most one per attribute); D-efficient search is a later refinement (ledger D-17).
- Currency/i18n, PNG/PPTX export, elasticity & substitution matrix readout, scenario version history. All listed in §12.

### 2.3 Explicitly out (any version)

Billing integration, live data connectors, accounts/auth, server-side anything, telemetry.

---

## 3. Architecture & stack

### 3.1 Decision

**Next.js (App Router) + React + TypeScript (strict) + Tailwind CSS.** Configured with `output: 'export'` for a fully static build — no Node runtime at request time, no serverless functions, no backend, ever, for v1. State in Zustand (+ zundo for undo); validation and schema versioning with Zod; scenario sharing via lz-string compressed URL hash; charts hand-rolled as React+SVG components; fonts via `next/font/local` (self-hosted Inter subset, no external network); tests with Vitest + fast-check + Testing Library + Playwright smoke; **deploy to Vercel** (owner-confirmed, S1) via the Next.js integration — the studio remains a plain static bundle behind Vercel's CDN.

### 3.2 Alternatives weighed

| Option | Verdict |
|---|---|
| **Next.js (App Router, `output: 'export'`, Vercel)** ✅ | Owner-fluent; App Router gives clean file-based structure without pulling in SSR; Next `output: 'export'` produces the same static bundle a plain Vite build would, but keeps `next/font`, `next/image`, and first-class TS/ESLint config on the shelf if we ever need them; Vercel deploys via repo-connect with zero config. Chosen (S1). |
| Vite + React + TS | Lighter toolchain, but reintroducing routing/asset-pipeline/font solutions we get free from Next; not owner-native. Rejected: owner fluency and free ecosystem win once we've decided against a backend anyway. |
| Vanilla ES modules | Maximally minimal, but this app is dense interdependent state (edit a WTP band → re-simulate → 8 views update). React's declarative model + TS types is materially safer for agent-built UI. Rejected for velocity and safety, not capability. |
| Chart library (Recharts/visx/d3) | Rejected as deps: ~18 chart instances, all bespoke (dot swarm, waterfall, envelope diagram, price-value map, tornado, PSM crossings — no library ships them). Hand-rolled SVG = full control, tiny bundle, and the craft is part of the showcase. `d3-*` micro-packages allowed later only via ledger entry. |
| Web Workers for Monte Carlo / MNL Newton-Raphson | Not needed by default: 1,000 MC draws over ≤ 6 segments × ≤ ~20 offers of closed-form arithmetic is sub-10 ms; MNL on ≤ 500 respondents converges in <10 Newton iterations in <100 ms. Revisit only if profiling disagrees (§7.6 perf gates). |
| Next SSR / server actions / RSC data fetching | Excluded by `output: 'export'` and by product stance (no server, no data). App Router is used purely as a static file-based structure. |

### 3.3 Module boundaries

```
app/            Next.js App Router routes:
  layout.tsx     root shell (header, footer, theme provider, aria-live region)
  page.tsx       studio (workbench + wind-tunnel; tabs are state, no sub-routes)
  globals.css    Tailwind entry
components/    UI components, views, charts (SVG), design tokens.
               May import lib/engine + lib/state.
lib/
  engine/      Pure TypeScript. No React, no DOM, no Date.now/Math.random
               (PRNG injected). Deterministic: same input → same output.
               ESLint-enforced: engine may not import from components/ or lib/state/.
    stats.ts       Φ, Φ⁻¹, erf, lognormal (pdf/cdf/quantile/partial expectation), band fitting
    offers.ts      offer expansion: tiers × add-on subsets + outside option (+ free tier + competitors)
    envelope.ts    upper-envelope selection: breakpoints, per-offer shares
    economics.ts   revenue, conversion, ARPA, surplus decomposition, capture rate,
                   counterfactual diffs (remove-offer), price sweeps
    montecarlo.ts  seeded PRNG (mulberry32), scenario draws, percentiles, tornado
    vanwest.ts     PSM validation, curve construction, intersection solving
    bundling.ts    regime comparison (components / pure bundle / mixed) + optimizer
    conjoint.ts    CBC: MNL log-likelihood + gradient + Hessian, Newton-Raphson estimator,
                   random-balanced design generator, price-attribute → WTP bridge
    maxdiff.ts     best-worst counting scores, normalization, design generator
    competitive.ts frontier (Pareto staircase, §4.11), iso-utility rays, competitor-record validation/mapping
    linter.ts      rule engine over scenario + simulation results
    (no barrel; modules are imported directly — ledger D-37)
  state/       Zod schemas (versioned), Zustand stores, persistence
               (localStorage autosave), JSON import/export, URL-hash codec.
content/      Glossary/explainer copy, linter messages with citations,
              template scenario data.
e2e/          Playwright smoke flows.
docs/         MODEL-SPEC.md (from Phase 1), screenshots.
public/       Static assets (favicon, font subset if not inlined via next/font).
```

**The load-bearing abstraction:** one primitive, `selectOffers(offers: {value, price}[], sigma, tieMode = 'conservative') → {activeOffers, breakpoints, shares}` (§4.2; `tieMode` is `'conservative'` everywhere in the app and `'seller-favorable'` only in the closed-form-oracle tests, D-20), consumed by tier simulation, free-tier counterfactuals, add-on choice, bundling regimes, *and* competitor alternatives alike. Every economics claim in the app reduces to this one tested function plus integration formulas (§4.3). This is deliberate: correctness concentrates where the tests are.

**Scenario data model:** one scenario = one buyer model + N designs (packaging variants) + optional competitor set + optional survey artifacts (PSM responses, Conjoint tasks/responses, MaxDiff tasks/responses) + settings `{seed, currency, theme}`. A/B compares designs *within* a scenario against the same buyers — apples to apples by construction. `settings.theme` is the single theme preference: the header control edits it, `'system'` follows the OS, and shared/imported scenarios intentionally carry it; there is no second hidden global override. Autosaved to localStorage and exportable as versioned JSON (maximum 2 MiB before parsing). The URL payload is a separate, compact schema: it carries Model/Design/competitor/settings state only, never respondent-level survey records or derived research results; the Share UI says so and offers full JSON export for a complete handoff. The encoded hash is capped at 8 KiB and its decompressed form at 64 KiB before parsing; oversized or corrupt payloads fail closed with an actionable export/import message.

### 3.4 Dependency budget (hold the line)

Production: `next`, `react`, `react-dom`, `zustand`, `zundo`, `zod`, `lz-string` — **7, budget full** (bumped from 6 in S1 to accommodate `next`; ledger D-19). Dev: typescript, tailwindcss, vitest, fast-check, @testing-library/react, playwright, eslint (with `eslint-config-next`), prettier, axe-core (via playwright). Exact versions pinned at Phase 0 scaffold time and recorded in the ledger (latest stable at scaffold time; don't inherit this doc's assumptions about versions).

### 3.5 Non-functional requirements

- **Performance:** edit → re-simulate → repaint < 16 ms typical (closed form makes this cheap); Monte Carlo 1,000 draws < 150 ms end-to-end (the closed-form compute alone is ~sub-10 ms per §3.2; the 150 ms figure is a *ceiling* that also covers percentile + tornado aggregation and leaves margin — not a target); Conjoint MNL fit on ≤ 500 respondents < 100 ms; total client JS bundle ≤ 320 KB gzip (CI-gated; the ceiling moved 250 → 300 KB in S1 to absorb the Next static-export runtime overhead, D-01/S1, and 300 → 320 KB in S29 to carry the v1.1 research extensions, D-34 — with actual target "as small as possible under Next's static-export path", hand-audited in P0). The v1.0 core alone remains expected to sit under the original 300 KB.
- **Privacy:** zero network calls at runtime, no external fonts/CDNs; Inter variable font subset via `next/font/local` (self-hosted). P0 enforces this two ways: static checks reject known network APIs/loaders, and a Playwright request allowlist fails any runtime request other than the document and same-origin static assets. The Vercel deployment sets `connect-src 'none'`; `docs/STATIC-HOST-HEADERS.md` records the equivalent header for portable hosts.
- **Accessibility:** WCAG 2.1 AA intent — full keyboard operation, every chart has a "view as table" toggle, `prefers-reduced-motion` respected everywhere, Okabe–Ito colorblind-safe categorical palette that meets contrast in both light and dark themes, axe CI gate with 0 serious/critical violations on both themes.
- **Determinism:** seeds visible and settable; identical scenario + seed → identical outputs, byte-stable exports (tested).

---

## 4. Economic engine & method catalog (the correctness contract)

Everything here is normative. Phase 1 extracts §4 into `docs/MODEL-SPEC.md` with section numbers preserved; every engine test cites the spec section it verifies (§7.3). Named tests (T-IDs) are the acceptance instruments — Opus reviews phases against them.

**Notation.** Segments `s` have prospect count `n_s`, seat count `c_s ≥ 1`, account-level per-feature median values `v[s,f] ≥ 0` ($/account/month for a representative account of `c_s` seats), and within-segment spread `σ_s`. Feature catalog `F`. An **offer** `o` has feature set `F_o` and list price `p_o`; flat `p_o` is $/account/month, while per-seat `p_o` is $/seat/month and has effective account price `P_s(o) = p_o · c_s`. Offer value `V_s(o) = Σ_{f ∈ F_o} v[s,f]` is always the account-level value for that segment (additive valuation — complementarity is v2, §12). The Model UI explicitly labels `W_s` as total account WTP at the stated seat count; it is not a per-seat WTP field.

### 4.1 Buyer model

Within segment `s`, an individual buyer draws a scale factor `ε ~ LogNormal(0, σ_s²)` (median 1) and values offer `o` at `ε · V_s(o)`. Buyer utility: `u(o) = ε·V_s(o) − P_s(o)`; outside option `u = 0`.

- Structure: **horizontal** differentiation across segments (different value *vectors*), **vertical** within a segment (one-dimensional scale ε). Within a segment, single-crossing holds by construction: offers rank identically for all buyers by `V_s`, and higher-ε buyers gain more from higher-V offers. This is a mixture of textbook vertical models (Mussa–Rosen / Tirole §3.5; Shapiro–Varian versioning), which is exactly the screening structure the product teaches.
- Input mapping: user enters median total WTP `W_s` for the full catalog and allocation shares `w[s,f]` (Σ_f w = 1) → `v[s,f] = W_s · w[s,f]`. Advanced view edits `v` directly.
- Two distinct uncertainty knobs, never conflated (UI must teach this): `σ_s` = *how much buyers within the segment differ* (analytic, §4.2); the P10/P90 **confidence bands** around base/P50 `W_s` and `n_s` = *how unsure the user is* (Monte Carlo, §4.8).
- Lognormal band contract: all base values and quantiles are strictly positive. Base `x` is P50, so the fitted band must satisfy `x = sqrt(q10·q90)`. The UI edits a symmetric log-space width around `x` (or mirrors the opposite bound when one bound changes), rather than accepting three inconsistent quantiles. Given `(q10, q90)`, `μ = (ln q10 + ln q90)/2 = ln x`, `σ = ln(q90/q10) / (2·z90)`, `z90 = 1.2815515655446004`. `q10 = q90 = x` is the explicit deterministic branch (`σ = 0`), not a validation error.

Tests — **T-STAT-01** Φ matches reference values to 1e-7: Φ(0)=0.5, Φ(1)=0.8413447461, Φ(1.96)=0.9750021049, Φ(−3)=0.0013498980. **T-STAT-02** Φ⁻¹(Φ(x))=x to 1e-9 on the interior [−6, 6] grid and to 1e-8 at the ±6 endpoints (D-32: binary64 rounding yields 8.85e-9 at +6). **T-STAT-03** band fit round-trips: quantile(fit(q10,q90), .1)=q10 and (.9)=q90 to 1e-9 rel. **T-STAT-04** lognormal partial expectation (§4.3) matches numeric integration (Simpson, 1e-6 rel) on 20 random (σ, a, b).

### 4.2 Offer selection — the upper envelope (the core primitive)

Buyer with scale ε chooses `argmax_o { ε·V_s(o) − P_s(o) }` including the outside option `(V=0, P=0)`. Exact ties (equal utility within the tie tolerance, §4.12) are resolved by an explicit **tie mode** — a parameter of `selectOffers`, default `'conservative'`:

- **`'conservative'`** — the product simulator everywhere. Break to **lower effective price, then lower value**, and prefer the outside option when a paid offer ties it. Conservative for revenue, deterministic, documented in-UI. This is the rule D-16 governs.
- **`'seller-favorable'`** — the closed-form-oracle validation harness only (§4.5 screening, §4.6 bundling), always at the σ→0 point-mass path. Break to **higher value, then higher price**, and prefer *participation* over the outside option on a tie. This is the δ→0 limit of pricing strictly inside each binding constraint: the textbook menus price served buyers exactly at a binding value, so the indifferent buyer is assumed to participate and take the intended (highest-value) offer. The two modes are exact mirror images.

**Exact own/competitor identity policy (D-31).** If an own offer and a competitor have identical effective `(V, P)`, attribute the full tied mass to the competitor before either tie mode is applied. This is the explicitly conservative policy: it does not inflate own revenue and preserves the five-term accounting by assigning the value to `CompetitorLoss`. Stable offer ID remains only the deterministic fallback among otherwise equivalent offers of the same owner class.

Running the screening/bundling oracle fixtures under `'conservative'` sends every indifferent buyer to the outside option and reports revenue far below the textbook result (see the guard in §4.5) — that is a *test-configuration* error, not an engine bug. Fixtures not specifically exercising tie behavior should price strictly inside participation (no exact ties) so they read identically under either mode (D-20).

In ε-space each offer is a line `u_o(ε) = V_o·ε − P_o`. The chosen offer as a function of ε is the **upper envelope** of these lines: a sequence of active offers with increasing V and increasing breakpoints
```
ε*(o_k → o_{k+1}) = (P_{k+1} − P_k) / (V_{k+1} − V_k)
```
Algorithm (convex-hull trick): dedupe equal-V offers keeping min P; sort by V ascending; stack-scan popping any line whose intersection with its predecessor occurs at or before the predecessor's own entry breakpoint. Offers never on the envelope have **zero share** (dominated — the linter reuses this, §4.9).

Share of segment s choosing active offer k, with `Φ_ln(x) = Φ(ln(x)/σ_s)` and breakpoint interval `(a_k, b_k]` clipped to ε > 0:
```
share_k = Φ_ln(b_k) − Φ_ln(a_k)
```
Degenerate case σ_s = 0: all mass at ε = 1; choose by direct argmax under the active tie mode (§4.2). The screening/bundling oracles (§4.5, §4.6) exercise exactly this path with `tieMode = 'seller-favorable'`.

**Offer expansion.** The simulated offer set per segment = paid/free tiers × subsets of ≤ 3 add-ons not already contained in the tier (composite value = union of features; composite price = sum), plus outside option, plus (when Positioning is active, §4.11) any competitor alternatives valued for that segment. Add-on purchase is therefore *jointly* optimal with tier choice — no greedy approximation. Cap: ≤ 5 tiers × 2³ add-on subsets + 1 outside + up to 6 competitors = ≤ 47 offers/segment.

Tests — **T-ENV-01** dominated offer (V ≤, P ≥ another) gets zero share for every σ. **T-ENV-02** breakpoints strictly increasing; shares sum to 1 (incl. outside) to 1e-12. **T-ENV-03** *brute-force cross-validation*: for 200 fast-check-random offer sets and σ values, sample 1,000 ε quantile points; direct argmax agrees with envelope interval assignment at every point (away from breakpoint ties by > 1e-9). **T-ENV-04** σ=0 reproduces point-WTP argmax with documented tie-breaking. **T-ENV-05** free tier `(V>0, P=0)`: outside option gets zero share for ε > 0; free absorbs the low-ε mass. **T-ADD-01** add-on expansion agrees with brute-force enumeration over tier × subset combos on random fixtures. **T-CMP-08** pins competitor-first own/competitor identity attribution and input-order independence.

### 4.3 Economics readouts & the conservation identity

With lognormal partial expectation `L(a,b) = E[ε·1(a<ε≤b)] = e^{σ²/2}·[Φ((σ²−ln a)/σ) − Φ((σ²−ln b)/σ)]` (limits: `ln 0 → −∞`), per segment s over active offers k on intervals `(a_k, b_k]`:

```
Revenue_s      = n_s · Σ_{k ∈ own tiers/add-ons} share_k · P_k
OwnBuyerSurplus_s = n_s · Σ_{k ∈ own offers (paid or free)} [ V_k · L(a_k, b_k) − P_k · share_k ]
FencingGap_s   = n_s · Σ_{k ∈ own offers (paid or free)} (V_full − V_k) · L(a_k, b_k)      where V_full = V_s(F)
Unserved_s     = n_s · V_full · L(outside interval)
CompetitorLoss_s = n_s · Σ_{k ∈ competitors} V_full · L(a_k, b_k)       (only when Positioning active)
Potential_s    = n_s · E[ε] · V_full ,   E[ε] = e^{σ²/2}
```

**Conservation identity (must hold exactly):** `Potential = Revenue + OwnBuyerSurplus + FencingGap + Unserved + CompetitorLoss`. This is deliberately an *our-catalog potential* identity: each buyer's `ε·V_full` is assigned to an own offer (price paid + buyer utility + features withheld), to non-conversion, or to a competitor. A free fenced offer participates in `FencingGap` even though its price is zero. Competitor utility/revenue is intentionally **not** folded into `OwnBuyerSurplus`; it may be reported separately as a non-conserved comparison metric, never in this waterfall. When no competitors are active, `CompetitorLoss = 0` and the identity collapses to the four-term form.

KPIs: `MRR = Σ_s Revenue_s`; `paid conversion = paid buyers to us / Σ n_s`; `ARPA = MRR / paid buyers to us`; **capture rate** `= MRR / Σ Potential_s` (headline metric); **competitor loss share** `= Σ CompetitorLoss / Σ Potential_s` (headline metric when Positioning is on). Counterfactual diffs: re-run with an offer removed (e.g., free tier, a competitor) → `Δ MRR` = that offer's leakage/contribution (used by linter E5, E7 and the add-on and competitor analyses).

Tests — **T-ECON-01** hand-computed 2-segment, 2-tier fixture matches all readouts to 1e-9. **T-ECON-02** (property, fast-check) conservation holds to 1e-6·Potential on random scenarios *with and without* competitor alternatives. **T-ECON-03** (property) raising one offer's price weakly decreases its own share. **T-ECON-04** analytic own-buyer surplus matches numeric integration on random fixtures (1e-6 rel). **T-ECON-05** counterfactual remove-offer: removing a zero-share offer changes nothing (to 1e-12). **T-ECON-06** has two hand-computed guards: a fenced free tier contributes its withheld value to `FencingGap`, and a competitor selection contributes only `CompetitorLoss` (not `OwnBuyerSurplus`); both preserve the identity exactly.

### 4.4 Price sweeps (demand & revenue curves)

For a selected tier, sweep its **list-price unit** over a 400-point base grid plus the exact current price, recomputing the envelope at each point. The initial upper bound is `max(current price, 1.5 × max_s V_full,s)` for flat pricing and `max(current price, 1.5 × max_s (V_full,s / c_s))` for per-seat pricing. If revenue is still rising at the upper boundary or the best point lies in the top 5% of the range, double the upper bound and recompute, up to 8 expansions. If the expansion cap is reached with a boundary optimum, label the result “best in searched range,” show the range, and do not imply an optimum. Changing a tier price shifts every composite offer derived from that tier; `Q(p)` and `R(p)` aggregate the selected tier plus those composites. Render current-price and best-in-range markers. This is the residual demand curve given the rest of the menu (and any active competitors), labeled as such.

Tests — **T-SWP-01** the exact current price is included and equals the scenario's simulated values (1e-9). **T-SWP-02** `Q(p)` weakly decreases in own price (property). **T-SWP-03** golden fixture: argmax within one grid step of independently computed optimum. **T-SWP-04** a high-spread fixture whose peak lies beyond the initial 1.5× bound triggers expansion and returns an interior best point; an expansion-cap fixture reports the boundary warning.

### 4.5 Second-degree price discrimination oracle (screening correctness)

The classic two-type menu result the engine must reproduce (Mussa–Rosen; σ→0). Types L, H; tiers Basic ⊂ Pro; values `V_LB, V_LP, V_HB, V_HP` with single crossing (`V_HB ≥ V_LB`, `V_HP − V_HB ≥ V_LP − V_LB`). Optimal serve-both menu:
```
p_B = V_LB                       (L's IR binds — full extraction on Basic)
p_P = V_HP − V_HB + V_LB         (H's IC binds — H keeps information rent V_HB − V_LB)
Exclude L entirely  iff  n_H·(V_HB − V_LB) > n_L·V_LB   (then p_P = V_HP)
```
Canonical fixture: `V_LB=40, V_LP=55, V_HB=60, V_HP=100`, `n_L=100, n_H=50` → `p_B=40, p_P=80`, revenue `8000`; exclusion variant `n_L=10` → Pro-only at `p_P=100`, revenue `5000 > 4400`.

**Tie-convention guard — read before implementing these tests.** This menu prices each served type exactly at a binding value (L's IR: `p_B = V_LB`; H's IC: `p_P` leaves H indifferent between the two tiers), so every served buyer sits on an exact tie. The engine reproduces the textbook revenue **only** under `tieMode = 'seller-favorable'` at the σ→0 point-mass path (§4.2). Under the simulator's default `'conservative'` mode the same fixture yields ≈2000 (L → outside on its IR tie; H → Basic on its IC tie), and σ=1e-3 under *any* mode yields ≈5000 (pricing at the segment median drops the below-median half). **If T-SCRN-01 does not return 8000, the tie mode / σ is the suspect — do not modify the envelope or selection engine to force the number.** The engine is correct; the test must run σ=0 with `'seller-favorable'`. (Verified numerically during review S2, D-20.)

Tests (all at **σ=0** with **`tieMode = 'seller-favorable'`** per the guard) — **T-SCRN-01** engine at (40, 80): L→Basic, H→Pro, revenue exactly 8000, L surplus = 0, H surplus = 20/buyer. **T-SCRN-02** perturb `p_P` to 85 → H switches to Basic (cannibalization; strict preference, no tie), revenue 6000. **T-SCRN-03** exclusion variant: menu {Pro@100} beats serve-both 4400, L takes outside option. **T-SCRN-04** no-distortion-at-top sanity: at the optimal menu, H buys the highest-value tier.

### 4.6 Bundling analyzer (Adams–Yellen on the envelope engine)

Question it answers: *should capability X be a tier fence, a separate add-on, or both/mixed?* Model two goods A, B (feature bundles) with per-segment values from the same matrix. Regimes as offer sets on the standard engine (§4.2):
```
Pure components : {A@p_A, B@p_B, A+B@(p_A+p_B)}     (buying both is allowed)
Pure bundle     : {A+B@p_AB}
Mixed bundling  : {A@p_A, B@p_B, A+B@p_AB}, p_AB ≤ p_A + p_B
```
Within-segment ε is perfectly correlated across goods (one scale factor), so bundling gains come from *across-segment* value dispersion — the honest SaaS interpretation, stated in-UI. Optimizer: exhaustive enumeration of the documented finite candidate grid for each regime's prices on the closed-form revenue (segment valuations ± spread quantiles, plus a "not offered" sentinel = price above all WTP); report the **best price found under the documented search grid**, regime deltas, and the searched candidates. Comparisons at given prices are exact; the UI never calls a grid-search result a global optimum.

Tests (closed-form optima T-BND-01…03 at **σ=0** with **`tieMode = 'seller-favorable'`**: pricing a bundle at exactly its value is a participation tie, and the textbook optimum assumes the buyer participates — same guard as §4.5) — **T-BND-01** canonical negative-correlation case: segments (A=9,B=1) and (A=1,B=9), equal sizes → pure components optimum 9/9 (rev 18·n), pure bundle 10 (rev 20·n); bundle wins. (Under `'conservative'` the pure-bundle-at-10 fixture yields 0 — the guard applies here too.) **T-BND-02** mixed ≥ max(components, pure bundle) at optima (mixed nests both; property, tolerance = grid resolution). **T-BND-03** σ→0 optimizer matches brute-force grid over type valuations on random 3-segment fixtures. **T-BND-04** the simulator's default `'conservative'` mode: between two *offered* options of equal utility the buyer takes the cheaper one — an offer-vs-offer tie, distinct from the participation tie the other tests exercise — documenting why "not offered" sentinels matter.

### 4.7 Van Westendorp Price Sensitivity Meter

Survey-first stance: the PSM module analyzes *fielded* responses (manual entry or CSV paste: 4 prices per respondent). A model-generated illustrative mode exists but is labeled "simulated from your assumptions — for intuition, not evidence" (mapping documented in-UI; ledger D-11).

Construction: per respondent quadruple `(too cheap ≤ cheap ≤ expensive ≤ too expensive)`; violators flagged and excluded (count shown — data hygiene is part of the pedagogy). Cumulative curves on the union grid of response prices: "too cheap" and "cheap" descending; "expensive" and "too expensive" ascending; `not cheap = 100% − cheap`, `not expensive = 100% − expensive`. Crossings by piecewise-linear interpolation:
```
PMC = too cheap × not cheap      PME = too expensive × not expensive
IPP = cheap × expensive          OPP = too cheap × too expensive
Acceptable range = [PMC, PME]
```
(Conventions vary in the literature; ours pinned to the standard operationalization of van Westendorp 1976, stated in the explainer.) No crossing → point reported "undefined for this data" — never fabricated.

Tests — **T-VW-01** monotonicity validation drops exactly the violating respondents on a fixture. **T-VW-02** symmetric synthetic dataset (quadruples mirrored around 50) → OPP = IPP = 50 exactly; PMC, PME symmetric about 50. **T-VW-03** hand-computed 5-respondent fixture matches all four points to 1e-9. **T-VW-04** no-crossing fixture returns undefined markers, not numbers.

### 4.8 Monte Carlo & tornado (assumption uncertainty)

Uncertain parameters: each segment's P50/base `W_s` (value scale) and `n_s` (size), via the P50-centered P10/P90 bands in §4.1 → lognormal draws; `σ_s` and prices held fixed. A zero-width band is a constant equal to the base value. PRNG: mulberry32, seed visible in UI and stored in the scenario. K = 1,000 draws (configurable 200–5,000); each draw is evaluated with the closed-form engine (no nested simulation). All A/B designs receive the **same indexed vector of parameter draws** (common random numbers), so a win rate is paired rather than a comparison of unrelated samples; an equal-MRR draw is a tie, not a win. Outputs: MRR distribution (P10/P50/P90 band), per-design; A/B win rate ("B beats A in 78% of draws"); tornado = one-at-a-time low/high (P10/P90) per parameter vs. base, bars sorted by |ΔMRR|. Tornado × provenance tags → the validation to-do list.

Tests — **T-MC-01** fixed seed → byte-identical percentiles and first 5 draws (snapshot). **T-MC-02** zero-width bands → P10 = P50 = P90 = analytic MRR exactly. **T-MC-03** MC mean within 3 standard errors of analytic value on a 1-uncertain-parameter fixture. **T-MC-04** tornado of a parameter with zero-width band has zero-length bar. **T-MC-05** two identical designs consume the same draw vector and produce 100% ties / 0% wins for either side.

### 4.9 Design linter (deterministic critic)

Every rule: id, exact trigger, severity, message, citation where applicable; all computed from the scenario + simulation results, no randomness. Economic rules:
- **E1 dead fence** — feature in no offer, or in all offers including free: fence carries no screening information. Info.
- **E2 dominated offer** — zero envelope share for every segment (§4.2): dead weight *or* deliberate decoy. Message explains asymmetric dominance honestly: the rational model scores it inert; behavioral evidence (Huber–Payne–Puto 1982) says a dominated decoy can lift the dominating tier — the model won't invent a number for that, but here's the trade-off to consider. Warning.
- **E3 fence inversion** — a higher-priced tier missing a feature a cheaper tier has (non-nested ladder): breaks the upgrade narrative; sometimes intentional ("good-better-different"), so warn with rationale. Warning.
- **E4 downgrade mass / cannibalization** — > 30% of a segment's buying mass chooses an offer with value below the segment's value-maximal tier; message names the price gap and fence causing it (read from breakpoints). Warning with $ quantification.
- **E5 free-tier leakage** — counterfactual remove-free re-run (§4.3): "free absorbs X% of would-be paid demand ≈ $Y MRR." Info/warning by threshold (default: warn > 15% of MRR).
- **E6 add-on cannibalization** — counterfactual remove-add-on: add-on's net MRR contribution after tier-mix shift; negative → warning.
- **E7 competitor loss (Positioning only)** — a segment loses > 25% of its Potential to a single competitor: message names the competitor and the tier(s) it beats on the envelope. Warning with $ quantification.
Behavioral rules (cited, deterministic):
- **B1 choice overload** — > 4 paid offers visible (Iyengar–Lepper 2000, framed as directional evidence with replication caveats). Info.
- **B2 weak anchor** — top tier takes < 2% share and < 1% of MRR: prune it, or reposition deliberately as an anchor/decoy (explainer covers anchoring; Tversky–Kahneman 1974). Info.

Tests — **T-LNT-01…09** one fixture per rule triggering it exactly, plus a clean fixture triggering none.

### 4.10 Conjoint (CBC) & MaxDiff-lite

**Purpose.** Estimate feature-level preference weights from choice data, then optionally bridge those weights into the value matrix. Positioned as "the survey-driven complement to the assumption-driven model" — the same pedagogical stance as PSM: your assumptions get pressure-tested by real choices.

**Choice-based conjoint (CBC).** User defines 1–5 non-price attributes with 2–4 levels. The bridge requires a **numeric price column** in $/account/month, the same unit as the value matrix; a categorical price attribute may be estimated and charted but cannot enable a WTP bridge. The design generator produces `T` choice tasks (default 12), each a set of `k` (default 3) concepts + an optional "None" alternative. Its random-balanced contract is achievable for arbitrary valid `T`, `k`, and level counts: within each non-price attribute, level appearance counts differ by at most one (exact equality only when `T·k` is divisible by that attribute's level count). D-efficient generation is a later refinement (§2.2, D-17). It rejects duplicate concepts within a task and validates full column rank before a study can be fielded. Respondent data entry is manual or CSV of `respondent_id, task_id, chosen_alternative`.

**Model.** Multinomial logit with effects-coded **non-price** part-worths. For attribute `a` and level `l`, part-worth `β_{a,l}` has identifying constraint `Σ_l β_{a,l} = 0`; implementation estimates `L_a−1` free coefficients and reconstructs the final level. For bridge-enabled studies, the numeric price coefficient `β_p` is utility per $/account/month. Concept `j` has utility `u_j = Σ_a β_{a, level(j,a)} + β_p·price_j`; None utility is `β_None`. Choice probability in set `S_t` for respondent `r`:
```
P(choose j | S_t; β) = exp(u_j) / Σ_{k ∈ S_t} exp(u_k)   (log-sum-exp for numerical stability)
```
Aggregate log-likelihood (pooled across respondents — HB deferred, §2.2):
```
ℓ(β) = Σ_r Σ_t log P(chosen_{r,t} | S_t; β)
```
`ℓ` is concave; it is strictly concave only when the design is full rank and the observed choices are not separated. Estimate via **damped Newton-Raphson** with analytic gradient and Hessian, backtracking only on likelihood-improving steps, to `‖∇ℓ‖∞ < 1e-8` or 50 iterations. Return one of `ok`, `nonIdentifiable`, `separated`, or `nonConverged`; only `ok` with invertible observed information may report `SE(β̂) = √diag(−H(β̂)⁻¹)`, 90% CIs, or a WTP bridge. The UI explains failure states instead of fabricating coefficients or CIs. Overall hit rate is training-set descriptive only, never validation accuracy.

**MaxDiff-lite** (importance scoring, no price required). User defines `m` total items (up to 12); the design generator produces tasks each showing `k` (3–5) items; the respondent picks best and worst per task. Before scoring, every defined item must have `times_i_appeared ≥ 1`; otherwise return a validation error naming the unseen items, never `NaN`. Analysis in the first MaxDiff release uses best–worst counting scores
```
s_i = (times_i_chosen_best − times_i_chosen_worst) / times_i_appeared
```
then linearly rescaled so `Σ_i normalized_s_i = 100` and `min ≥ 0` (shift-then-normalize: subtract `min_i s_i`, then scale to sum 100). **Degenerate branch (required):** when all raw `s_i` are equal — the symmetric case that T-MXD-01 feeds — the shift makes every value 0 and the sum-to-100 scaling divides by zero; return the uniform distribution `100/m` across the `m` total scored items. Full MNL best-worst estimation is a later refinement.

**Bridge to the value matrix.** If the study has numeric account-month price, status `ok`, and `β̂_p` is significantly negative (`β̂_p + 1.645·SE(β̂_p) < 0`), a non-price part-worth contrast converts to account-month WTP: for a feature-attribute contrast between level `l₁` and `l₀`,
```
ΔWTP = −(β̂_{a,l₁} − β̂_{a,l₀}) / β̂_p
```
Because the first release estimates one pooled `β`, each `ΔWTP` is an **aggregate proposal**, not a respondent- or segment-specific estimate. Before applying, the user must map each chosen contrast to a catalog feature, explicitly select the segment cell(s) that should receive that aggregate proposal, and confirm the account-month period. The UI shows a separate proposed diff for every selected `v[s,f]` cell and never overwrites silently. Confirmed cells receive `pooled conjoint (N=<respondents>)` provenance. Per-segment refits require their own rank, separation, sample-size, and SE gates and are deferred; respondent mapping alone cannot manufacture segment part-worths. Categorical/no price, a non-negative or insignificant price coefficient, or any non-`ok` status disables the bridge while keeping relative part-worths visible.

Tests — **T-CNJ-01** recovery: simulate 500 respondents from a known `β` on a full-rank 3-attribute × 3-level design with numeric price → estimated `β̂` within 3·SE of truth on every coefficient. **T-CNJ-02** log-likelihood strictly increases on every accepted Newton step until the gradient stop condition. **T-CNJ-03** the observed Hessian is negative-definite at the `ok` optimum of that full-rank, non-separated recovery fixture. **T-CNJ-04** effects-coding constraint `Σ_l β̂_{a,l} = 0` holds to 1e-10 per attribute in the returned estimate. **T-CNJ-05** an exactly balanced β=0 fixture returns near-zero part-worths and no bridge. **T-CNJ-06** bridge is disabled when numeric `β̂_p` is not significantly negative. **T-CNJ-07** rank-deficient and separated fixtures return the named status, no CIs, and no bridge. **T-CNJ-08** generated level counts differ by at most one for every attribute across valid non-divisible `T·k` fixtures. **T-MXD-01** best-worst counting: symmetric fixture across `m` total items → all scores equal (`100/m`) after normalization via the degenerate branch. **T-MXD-02** normalized scores sum to 100 ± 1e-9 and are non-negative. **T-MXD-03** an item with zero appearances returns the named validation error and no scores.

### 4.11 Competitive price-value map & competitor alternatives

**Two coupled surfaces powered by the same data.**

**(1) Positioning map.** Values vary by segment, so the map is explicitly **segment-scoped**: the user selects one segment and every competitor/tier point is plotted with that segment's value and effective price. Axes are value on X and price on Y. A zero-utility ray obeys `P = εV`, so its slope is `ε`, never `1/ε`. Because every segment has median `ε=1`, the UI shows selected-segment P10/P50/P90 rays (derived from that segment's `σ`) rather than pretending there are different median rays. Competitor points are the Pareto non-dominated set, not a convex hull: competitors are discrete alternatives and cannot be mixed (D-21). The map may draw a step-style visual connector, but no line is a market offer.

**(2) Competitor as an alternative in the envelope engine.** Each competitor becomes a per-segment offer `(V_s(competitor), P_competitor)` in that segment's offer set (§4.2), with a flag marking it as a competitor. Simulator computes:
- **Choice share going to a competitor** in a segment = **switching/loss risk** for that segment (headline number).
- Revenue accounting: competitor choice does **not** contribute to `Revenue_s`; it feeds `CompetitorLoss_s` in the conservation identity (§4.3). Our KPIs (MRR, capture rate) stay uncontaminated; a separate "competitor loss share" KPI surfaces when Positioning is active.
- Wind tunnel dot panel adds competitor swatches so churn/loss is *visible*, not just aggregated.

**Frontier construction and verdict.** Given competitor points `{(V_i, P_i)}`, drop dominated points (`V_i ≤ V_j ∧ P_i ≥ P_j` for some j, strict in one component), then sort survivors by V ascending. There is no linear interpolation and no vertical "distance to frontier": either would invent an unavailable competitor offer. A tier receives `directly dominated` only when a competitor has `V_c ≥ V_t ∧ P_c ≤ P_t` (strict in one); otherwise it receives `not directly dominated`. The envelope-engine competitor share remains the quantitative trade-off measure.

Tests — **T-CMP-01** frontier construction: 5-competitor canonical fixture matches an independently computed Pareto set — include a competitor that is Pareto-efficient but off the convex hull, to pin the discrete choice; single-competitor edge case returns that competitor. **T-CMP-02** a ray at slope `ε` passes through `(V, εV)` and a selected segment's P10/P50/P90 slopes match its lognormal quantiles. **T-CMP-03** duplicate competitor points collapse in the frontier (no double-count). **T-CMP-04** direct-dominance verdict triggers only for the stated discrete predicate; a point between two competitors is never judged by interpolation. **T-CMP-05** competitor as engine offer: on a fixture where a competitor priced below every tier at similar value exists, competitor share exceeds 0, paid conversion to us falls, all offer/outside shares still sum to 1, and the five-term conservation identity holds. The test does **not** assert a zero delta in `paid conversion + competitor share`: a competitor may attract previously unserved buyers. **T-CMP-06** identity preservation: enabling Positioning with an empty competitor set leaves all KPIs and shares unchanged (to 1e-12). **T-CMP-07** conservation with competitors: `Potential = Revenue + OwnBuyerSurplus + FencingGap + Unserved + CompetitorLoss` holds on random fixtures with 0–5 competitors (property). **T-CMP-08** pins competitor-first attribution of an own and competitor offer with identical `(V,P)` and proves input order cannot change attributed shares (P1) or KPIs (P2+).

### 4.12 Numerical conventions

Money as floating dollars (modeling tool, not accounting; display-rounded); prices ≥ 0; `σ_s` UI range [0.05, 2.0] with labeled presets (Low 0.25 / Medium 0.5 / High 0.9); tie tolerance 1e-9 (equal utility within this is a tie, resolved by the active tie mode, §4.2); percentages to 1 dp; currency USD-formatted via `Intl.NumberFormat` (multi-currency v2). Φ via a rational-approximation implementation tested to 1e-7 (T-STAT-01); Φ⁻¹ via Acklam/AS-241, tested by round-trip. Softmax via log-sum-exp for numerical stability (Conjoint MNL). All engine randomness flows through the injected seeded PRNG.

---

## 5. Interaction, visualization & UX

### 5.1 Layout — the loop on one screen

```
┌────────────────────────────────────────────────────────────────────────┐
│ ◆ name   [Scenario ▾]   Model · Design · Analyze · Share      seed 42  │
├──────────────────────────────────┬─────────────────────────────────────┤
│ WORKBENCH (Model or Design tab)  │ WIND TUNNEL (always on, always live)│
│                                  │  MRR · capture · conv · comp-loss   │
│ Model: segment cards (size,      │  ┌ buyer dots: segments → offers ┐  │
│  seats, WTP band, spread),       │  └ value waterfall ──────────────┘  │
│  value matrix (totals + shares;  │  per-tier price-sweep curves        │
│  advanced: direct $ edit),       │  [Mechanism view]  [Compare A/B]    │
│  provenance tags                 │                                     │
│ Design: tier cards, fence grid,  │  Linter findings dock (badge count) │
│  prices, metric, add-ons         │                                     │
└──────────────────────────────────┴─────────────────────────────────────┘
```
**Shipped in v1.0 (ledger D-35):** the wind tunnel is the Simulate workbench rather than a persistent desktop rail; Model carries a live engine-backed KPI rail and Design carries the live linter dock, so every edit still has an immediate on-screen consequence.
The wind tunnel never leaves the screen on desktop: **every edit re-simulates instantly** (closed form makes this free) — the product thesis as an interaction. Analyze — Uncertainty (Monte Carlo/tornado), Research (PSM, Conjoint/MaxDiff, Bundling), and Positioning (competitive map + competitor share) are full-screen views fed by the same scenario. Share (pricing page, decision record) also full-screen. Narrow viewports: tabbed, results one tap away. Tabs are state; the URL hash is reserved for the scenario payload.

### 5.2 Making the economics tangible

- **Buyer dots:** 20 dots per segment (1 dot = 5% granularity, disclosed in-surface; ledger D-36), colored by segment (Okabe–Ito), arranged by chosen offer; competitor offers get distinct hatched swatches so switching is legible at a glance. On change dots re-sort with a short eased transition. Reduced motion: instant reposition + numeric deltas. This is self-selection made visible.
- **Value waterfall:** Potential → Revenue / Own-buyer surplus / Fencing loss / Unserved / (Competitor loss when active), per §4.3's identity — the money shot for "surplus captured vs left on the table vs walking to the competitor." Competitor buyer utility, when shown, is explicitly a separate comparison metric rather than a waterfall term.
- **Mechanism view (toggle):** the actual screening diagram — utility lines vs ε, upper envelope shaded, breakpoints labeled, segment density strip beneath. The textbook figure, live. For expert credibility; also the best debugging view.
- **Price sweeps:** small-multiple R(p) and Q(p) per tier with current-price and grid-argmax markers.
- **A/B:** side-by-side KPI columns with delta chips, dot-panels, and MC win rate; "promote B to active" action.
- **Conjoint part-worth chart:** horizontal bar per attribute-level with 90% CI whiskers; hit-rate badge; "apply bridge" primary CTA gated on the conditions in §4.10.
- **PSM curve panel:** four cumulative curves with crossing markers and shaded acceptable range; a violator-count chip; illustrative-mode toggle labeled "SIMULATED — not evidence."
- **Positioning map:** segment selector; 2D chart with selected-segment P10/P50/P90 break-even rays, Pareto competitor points, our tier dots, and a discrete direct-dominance verdict. The simulator's competitor share, not an interpolated chart distance, is the trade-off number.
- **Explainers:** every loaded term (WTP, fence, surplus, decoy, capture rate, PMC, part-worth, iso-utility ray…) gets a popover from `content/glossary.ts` — one consistent voice, citations where claims are empirical. The educational layer is content, not chrome.

### 5.3 First-run & empty states

First visit: template picker (3 archetypes, §2.1) or "start blank"; templates load pre-filled with provenance `guess` and a visible note that the values are illustrative. The `benchmark` tag is reserved for a value with a recorded source note. Every empty state says what to do next. A worked default scenario means the hero view is alive within seconds of first load.

### 5.4 Accessibility & motion (acceptance-tested, §7.6)

Keyboard-complete flows (matrix grid = arrow-key navigable with visible focus); all charts paired with a "view as table" toggle rendering the same numbers; KPI updates announced via a debounced `aria-live` region; color pairs pass contrast on both fills and text in **both light and dark themes**; `prefers-reduced-motion` disables all transitions (state changes remain fully legible); print stylesheet for the decision record.

### 5.5 Visual language

Restraint: neutral slate surfaces with **light + dark themes**, one accent, Okabe–Ito for categorical segment color (validated for AA contrast against both surfaces), self-hosted Inter subset via `next/font/local`, generous whitespace, numbers formatted like a good board deck (USD, 1-dp percentages, no seven-decimal floats). No emoji, no gradients-as-personality, no dashboard clutter. Theme is a scenario setting (`settings.theme: 'system' | 'light' | 'dark'`); the header control edits that setting and `'system'` follows the OS. Shared scenarios intentionally preserve it. The Okabe–Ito palette gets subtly re-toned per theme so segment colors stay distinct on both backgrounds (ledger D-13).

---

## 6. Phased build plan

Sizing: S ≈ half an agent-session, M ≈ one, L ≈ two. Every phase ends with the §7.5 gate. Versions: minor bump per phase completion, patches within a phase (§8.3). "Review" = what Opus should scrutinize hardest.

**Global definition of done (every phase):** `npm run typecheck && npm run lint && npm run test && npm run build` green; no console errors/warnings in dev flows touched; CONTEXT.md session log + ledger updated; version bumped in `package.json` and footer; **stop and propose the exact version + commit message to the owner (§0 rule 4)** — do not run `git commit` until owner approves both.

---

**P0 — Scaffold, CI, deployed shell** · S/M · → v0.1.0
Next.js (App Router) + TS strict + Tailwind (with light + dark tokens defined in P0), `output: 'export'` for static build; ESLint (`eslint-config-next` + a custom **engine-purity import rule** blocking `lib/engine/*` from importing `components/*` or `lib/state/*`) + Prettier; Vitest + fast-check + Testing Library; Playwright (chromium); `.nvmrc` (current LTS); scripts (`dev/build/test/e2e/typecheck/lint/spec-coverage` stub). GitHub Actions: PR = typecheck+lint+test+build+bundle-size gate; main = Vercel deploys via the repo-connect integration (no CI-side deploy step; Vercel's own build runs `next build` and serves the static export). Add Vercel CSP headers (`connect-src 'none'`) and `docs/STATIC-HOST-HEADERS.md` for portable hosts. App shell: header (theme toggle + seed chip), tab skeleton, footer with version string from `package.json`. Manual scaffold (not `create-next-app`) — the dir is non-empty; don't clobber existing docs.
*Accept:* CI green on a test PR; shell deployed to public Vercel URL (needs owner's Vercel connection, §11); `README` run instructions work from clean clone; bundle gate active (≤ 300 KB gz, trivially passing now); theme toggle flips both light and dark tokens correctly on the empty shell; Playwright request allowlist proves the exported app makes no request beyond same-origin document/static assets.
*Review:* toolchain versions recorded in ledger D-19b; engine-purity lint rule actually fails on a violation (prove with a scratch test that then deletes); Next `output: 'export'` is set and no server-only features (`headers`, `cookies`, `revalidate`, image-optimization remote loaders) leak into `app/`.

**P1 — Engine: statistics + envelope core** · M · → v0.2.0
`stats.ts` (erf/Φ/Φ⁻¹, lognormal pdf/cdf/quantile/partial expectation, band fit), `offers.ts` (expansion incl. add-on subsets, per-seat effective pricing, and **functional** competitor-offer synthesis — a competitor is just another `(V_s, P)` offer carrying a competitor flag; building it for real here, not as a stub, is what lets the E7 linter rule be tested in P5 before the Positioning UI lands in P7e), `envelope.ts` (selection, shares, σ=0 path, **both tie modes** per §4.2). Extract §4 → `docs/MODEL-SPEC.md` (section numbers preserved). `spec-coverage` script: greps `@spec §x.y` tags in tests → coverage table; CI-reported.
*Accept:* T-STAT-01…04, T-ENV-01…05, T-ADD-01, and owner-resolved T-CMP-08 pass; spec-coverage shows §4.1–4.2 covered; engine imports nothing from components/state (lint proves); `selectOffers` exposes both tie modes (default `'conservative'`) and a unit test asserts they diverge on a constructed exact-tie fixture (both a participation tie and an offer-vs-offer tie).
*Review:* brute-force cross-validation test (T-ENV-03) is genuinely independent (no shared helper with the envelope implementation); numerical edge cases (σ→0, equal-V offers, free tier).

**P2 — Engine: economics readouts** · M/L · → v0.3.0
`economics.ts`: KPIs, surplus decomposition + conservation (both 4-term and 5-term with competitors), counterfactual remove-offer diffs, unit-correct price sweeps. Screening oracle fixtures.
*Accept:* T-ECON-01…06, T-SWP-01…04, T-SCRN-01…04 pass; spec-coverage §4.3–4.5 complete.
*Review:* conservation property test uses adversarial fast-check generators (fenced free tiers, dominated offers, σ extremes, with and without competitors); waterfall term definitions match §4.3 exactly.

**P3 — Scenario schema, state, persistence, sharing** · M · → v0.4.0
Zod schemas (versioned, `schemaVersion: 1`, migration stub), Zustand stores + zundo undo, localStorage autosave (debounced), JSON export/import with validation errors surfaced, URL codec `#s=` (lz-string), template data files (3 archetypes, §2.1) as validated fixtures. Persisted full-scenario JSON may include survey artifacts; the URL `SharePayload` schema cannot. It has an 8 KiB encoded cap plus decompressed-byte guard, and the UI directs a complete scenario to JSON export/import. Schema includes competitor list slot + survey-artifact slots even though those layers ship later — versioning gets done once.
*Accept:* T-SCH-01 export→import round-trip byte-equal; T-SCH-02 corrupted/foreign or over-limit JSON rejected with human message; T-URL-01 compact hash round-trip incl. unicode segment names; T-URL-02 oversized/decompression-bomb hash is rejected without parsing and offers JSON export/import; templates validate against schema; undo/redo works across a scripted edit sequence (unit).
*Review:* schema is minimal-but-sufficient for §2.1 scope; no engine types leak into persisted schema (versioning safety); sharing never silently drops respondent data.

**P4 — Model surface** · L · → v0.5.0
Segment cards (base/P50 size + P10/P90 confidence band, seats, base/P50 WTP + P10/P90 confidence band with live density strip, spread preset, provenance tag), value matrix (totals × allocation shares; advanced direct-$ grid), template picker + first-run flow, glossary popovers (initial set). Wind-tunnel rail may show placeholder KPIs wired to the engine (no charts yet). Theme toggle (light/dark/system) wired to state.
*Accept:* Playwright E2E-01: first visit → pick template → edit a WTP median → KPI text updates; axe 0 serious violations on both themes; matrix fully keyboard-navigable (e2e asserts focus traversal); size and WTP band inputs enforce the P50-centered contract in §4.1, reject nonpositive values or q10 > q90 inline, and accept q10 = q90 = base as deterministic.
*Review:* the σ-vs-confidence-band distinction is legible in the UI copy (the classic conflation, §4.1); input formatting (currency, %) consistent; theme switch survives reload.

**P5 — Design surface + linter** · L · → v0.6.0
Tier cards (name, price, metric flat/per-seat), fence grid (feature × tier checkboxes), free-tier toggle, add-ons (≤ 3), multiple designs per scenario (create/duplicate/rename), `linter.ts` + findings dock with cited explainers.
*Accept:* T-LNT-01…09 pass (E7 competitor loss asserts against a fixture with a competitor added directly to the scenario JSON, run through the **functional** competitor-offer synthesis built in P1 and the 5-term conservation from P2 — the full Positioning UI lands in P7e, but the engine path and the *rule* are real and testable now); E2E-02: build a 3-tier design from blank, see linter fire and clear; dominated-offer fixture shows E2 with per-segment detail; counterfactual $ figures match engine diffs (unit-checked against `economics.ts`).
*Review:* linter triggers exactly per §4.9 (no fuzzy thresholds beyond documented ones); messages are decision-useful, not scoldy.
*Status:* locally complete and verified in S17; owner review, release versioning, and commit direction remain pending.

**P6a — Wind tunnel: core reveal** · L · → v0.7.0
Live KPI header (MRR, conversion, ARPA, capture rate; competitor-loss placeholder that becomes live in P7e), buyer-dot panel (transition + reduced-motion path), value waterfall, per-tier price sweeps, "view as table" toggles, aria-live updates.
*Accept:* E2E-03: price edit → dots re-sort, waterfall + KPIs update; reduced-motion e2e (emulated) shows no transition but same end state; every chart has a table toggle with matching numbers (spot-assert two); perf: simulate+render interaction < 16 ms on templates (measured via Playwright trace, budget documented).
*Review:* charts hand-rolled cleanly (no dep creep); numbers in views reconcile with engine output exactly (one source of truth).
*Status:* locally complete and committed as v0.7.0…v0.7.3 (S20). S19 records 56 unit tests, four Chromium flows, zero serious/critical axe findings for the new flow, a 272.8 KiB gzip bundle, and warmed template simulation/render tasks of 4.4–5.6 ms against the 16 ms budget.

**P6b — Mechanism view + A/B compare** · M · → v1.1 extension (after v1.0)
Envelope diagram (lines, shaded envelope, breakpoints, density strip), A/B: design switcher, side-by-side KPIs + deltas, promote-to-active.
*Accept:* E2E-04: duplicate design, change a price, compare view shows correct deltas (engine-verified in test); mechanism view matches envelope.ts breakpoints (rendered labels asserted against engine values).
*Review:* mechanism view correctness — this is the expert-credibility surface; wrong here is fatal.

**P7a — Monte Carlo + tornado** · M · → v0.8.0
`montecarlo.ts` + Analyze — Uncertainty view: MRR band, per-design distributions, A/B win rate, tornado sorted by |ΔMRR|, validation to-do list (tornado × provenance).
*Accept:* T-MC-01…05 pass; E2E-05: set bands, run MC, tornado renders, seed change → different but deterministic results (re-run same seed → identical); 1,000 draws < 150 ms (measured).
*Review:* determinism end-to-end (no stray Math.random); statistical claims in copy are accurate (percentiles, not CIs).

*Status:* locally complete and committed as v0.8.0…v0.8.3 (S21). S21 records 63 unit tests, five Chromium flows, zero serious/critical axe findings for the new flow, a 277.3 KiB gzip bundle, and the 1,000-draw browser gate under 150 ms.

**P7b — Van Westendorp** · M · → v0.8.x
`vanwest.ts` + PSM view (Analyze — Research): manual/CSV input, validation report, four-curve chart, crossing markers, acceptable-range shading, illustrative-mode toggle (labeled), demo dataset.
*Accept:* T-VW-01…04 pass; E2E-06: paste demo CSV → points match fixture values; violator count displayed; no-crossing data shows "undefined" state.
*Review:* interpolation and convention choices match §4.7 exactly; illustrative mode cannot be mistaken for real evidence (labeling).
*Status:* locally complete and committed as v0.8.4…v0.8.6 (S23). S22 records 70 unit tests, six Chromium flows, zero serious/critical axe findings for the new flow, and a 281.9 KiB gzip bundle.

**P7c — Bundling analyzer** · M · → v1.1 extension (after v1.0)
`bundling.ts` + view (Analyze — Research): pick goods A/B from catalog/add-ons, per-regime optima, comparison verdict with revenue deltas and "prices searched" disclosure.
*Accept:* T-BND-01…04 pass; E2E-07: canonical fixture reproduces bundle-beats-components verdict.
*Review:* optimizer honesty (multi-start coverage, "not offered" sentinels); regime nesting property holds in UI-reported numbers.

**P7d-1 — Conjoint estimator core + MaxDiff scoring** · M · → v1.1 extension (after v1.0)
Estimation only, no UI. `conjoint.ts`: MNL log-likelihood + **analytic gradient + analytic Hessian** (log-sum-exp softmax), damped Newton-Raphson to `‖∇ℓ‖∞ < 1e-8` or 50 iterations, explicit status outcomes, SEs only from invertible observed information, effects coding for non-price attributes, numeric account-month price for a bridge-enabled study; random-balanced design generator; pooled price→WTP proposal function (not yet wired to the value matrix). `maxdiff.ts` (design generator, best-worst counting, normalization incl. zero-appearance validation and the degenerate branch, §4.10).
*Accept:* T-CNJ-01…08, T-MXD-01…03 pass. **Derivative gate (mandatory, precedes the recovery test):** with documented central-difference step `h = 1e-5·max(1, |β_i|)`, each analytic derivative must satisfy `absError ≤ 1e-7 + 1e-6·max(1, |finiteDifference|)` on several β/design fixtures. Normalizing the objective and derivatives per observation is also acceptable if used consistently. This localizes a sign/index bug before T-CNJ-01's global recovery test without false-failing large samples on absolute scale alone.
*Review:* the analytic gradient/Hessian is the single highest-risk piece of engine math. Do not weaken the derivative gate. Negative-definiteness is asserted only on the full-rank, non-separated fixture; rank-deficient/separated data must return a named failure status.

**P7d-2 — Conjoint + MaxDiff views + bridge** · M · → v1.1 extension (after P7d-1)
Analyze — Research views: CBC — attribute/level definer, task designer, respondent-response entry (manual + CSV), part-worth bar chart with 90% CI whiskers, hit-rate badge, "apply to value matrix" bridge action (gated per §4.10); MaxDiff — item list, task designer, entry, importance bar chart. Provenance stamp `pooled conjoint (N=…)` written to affected value-matrix cells.
*Accept:* E2E-08: run a 3-attr × 3-level CBC on the shipped synthetic dataset → recover part-worths within 3·SE; apply bridge → explicitly selected value-matrix cells update with pooled provenance; disabled-bridge tooltip appears when price attribute is removed; MaxDiff normalized scores displayed correctly.
*Review:* bridge messaging honest — "pooled conjoint-inferred WTP under the modeled attribute levels," not "true" or segment-estimated WTP. Within this v1.1 phase, a read-only part-worth chart may ship before the bridge action; P7d-1 remains engine-only and is never marketed by itself.

**P7e — Positioning: competitive map + competitor-in-envelope** · M · → v1.1 extension (after v1.0)
`competitive.ts` (competitor-record validation/mapping, Pareto point set, selected-segment break-even rays) and the Positioning view: competitor entry (name/price/per-segment value with an "overall" default), selected-segment 2D map with rays + points + tier dots + direct-dominance verdict, per-segment competitor-share readouts wired into the wind-tunnel dot panel and KPI header (competitor-loss share). Reuse the single competitor-offer expansion path implemented in `offers.ts` during P1; do not duplicate it here.
*Accept:* T-CMP-01…08 pass; E2E-09: add 3 competitors, select a segment, map renders in both themes, per-segment competitor-share appears in wind-tunnel dot panel and KPI header; toggle "include competitors in simulation" cleanly on/off with all KPIs restoring identically (T-CMP-06).
*Review:* competitor share does not corrupt Revenue/Potential accounting (competitors contribute to CompetitorLoss, never to OwnBuyerSurplus); map never interpolates a fake competitor offer; competitor-loss KPI only shown when at least one competitor exists.
*Status:* locally complete and uncommitted (S31); suggested release `v0.16.0`. 124 unit tests, five Chromium flows (plus E2E-09 tri-browser green), spec coverage requires §4.11, and the 307.8 KiB / 320 KiB gzip bundle all pass.

**P8 — Pricing Decision Record** · M · → v0.9.0
Pricing Decision Record generator: assumptions + provenance, design summary, KPIs + waterfall, sensitivity summary + validation to-dos, linter findings, alternatives (other designs' KPIs), seed + date; optional research/positioning sections appear only when their extension is present. Export as Markdown download and print-CSS PDF.
*Accept:* E2E-10: generate record → markdown file downloads, contains the scenario's actual numbers (asserted); print stylesheet produces sane single-doc layout (manual check + snapshot of print CSS applied).
*Review:* the record reads like a strategy artifact; no invented claims — everything traceable to engine output; sections gracefully omit when their source extension is absent.

**P8b — Pricing-page mock** · M · → v1.1 extension (after v1.0)
Pricing-page mock rendered from the active design (clean, copy-ready, theme-aware).
*Accept:* mock renders all tiers/fences of templates correctly in both themes.
*Review:* it remains a presentation layer over the active design, never a second source of pricing truth.

**P9 — Hardening & v1.0.0** · M · → v1.0.0
Full automated and manual accessibility pass on all views in both themes; cross-browser (Chromium/Firefox/WebKit smoke); perf budgets re-verified; content/copy edit pass; README rewrite with screenshots + live Vercel URL; MODEL-SPEC final read-through against implementation; template polish; release checklist.
*Accept:* all suites green incl. full e2e matrix; spec-coverage = 100% of MODEL-SPEC sections; bundle ≤ 320 KB gz (D-34; ≤ 300 KB if the v1.1 research extensions are excluded); axe 0 serious/critical anywhere in either theme; a recorded manual matrix covers complete keyboard workflows, focus order/restoration, one VoiceOver or NVDA screen-reader smoke path, non-color cues and manual contrast, 200% text zoom, 400%/320-CSS-px reflow, forced-colors legibility, reduced motion, and practical pointer targets; a stranger can go URL → defensible decision record in < 15 minutes using only in-app guidance (owner user-test). README may say “WCAG 2.1 AA intent” before this gate and “audited” only after evidence is recorded.
*Review:* end-to-end coherence — does the whole thing feel like one product; is anything shippable-embarrassing left.

**Release contract (not a fallback).** The **v1.0 required core** is P0–P6a, P7a, P7b, P8's Pricing Decision Record, and P9 hardening/sign-off. It is built end-to-end before any extension. **v1.1 extensions**, in dependency order, are P6b, P8b, P7c, P7d-1 then P7d-2, and P7e. A core dependency may land before its public extension (for example, competitor offers support E7), but an engine without its view is not marketed as shipped. This contract supersedes earlier broad "in v1" wording.

**Extension order.** After v1.0 is green, add P6b mechanism/A-B, P8b pricing-page mock, P7c bundling, P7d-1 estimator before P7d-2 views/bridge, and P7e positioning as individually reviewed v1.1 increments. The core loop is not cuttable; extension order may change only through a ledger entry that preserves dependencies.

---

## 7. Test & verification spine

1. **L0 — numeric primitives:** reference-value tests for Φ/Φ⁻¹/partial expectations (T-STAT-*). Nothing downstream is trusted until these pass.
2. **L1 — closed-form unit tests:** every §4 module vs hand-derived or literature results (T-ENV/ECON/SCRN/SWP/BND/VW/MC/CNJ/MXD/CMP/LNT-*). Test IDs are stable and cited in code comments.
3. **L2 — property/invariant tests (fast-check):** conservation identity (including fenced free tiers and competitors), share simplex, own-price monotonicity, envelope-vs-brute-force agreement, dominated-offer nullity, MNL concavity/status handling (negative-definite Hessian only on identified fixtures), and Pareto-set monotonicity. These catch what example-based tests can't.
4. **L3 — golden scenarios:** the 3 templates + adversarial fixtures + a canonical CBC fixture + a canonical Positioning fixture snapshot full engine output (JSON, seeded). Any diff is a reviewable event, not noise.
5. **L4 — UI tests:** Testing Library for stateful components (matrix editing, undo, part-worth-bar apply-bridge flow); Playwright smoke flows E2E-01…10 (< 3 min total, chromium in CI, tri-browser at P9).
6. **L5 — CI gates:** typecheck, lint (incl. engine purity + `eslint-config-next` rules), all tests, bundle size, axe (serious+, both themes), spec-coverage report.

**Spec-citation discipline:** every engine test carries `@spec §x.y`; `scripts/spec-coverage.mjs` fails CI if a MODEL-SPEC section has zero citing tests (from P2 on). Coverage target: `lib/engine` ≥ 90% branches — but the named T-tests, not the percentage, are the real bar.

**7.5 Phase gate protocol:** agent runs the global DoD commands + the phase's acceptance list verbatim → records results in the session log (§13) → updates ledger/backlog → **proposes exact version + commit message per §0 rule 4** → stops. Opus reviews against the acceptance list + review-focus line. Owner gives the commit ask (§8.3). Any acceptance failure = phase stays open.

**7.6 Non-functional gates:** perf numbers, bundle size, and axe results (both themes) recorded per phase in the session log; regressions block the gate.

---

## 8. Repo conventions

### 8.1 Structure (target once P0 lands)

```
README.md          public-facing: what/why/run/deploy/stack (outline: §8.2)
CONTEXT.md         this doc: plan, spec pointer, ledger, backlog, protocol, log
LICENSE            MIT — Sidakpreet Singh
package.json       Next.js app; version renders in footer
next.config.ts     output: 'export', no image loaders, no remote origins
app/globals.css    Tailwind v4 entry and light/dark theme tokens
tsconfig.json      strict
docs/              MODEL-SPEC.md (P1+), STATIC-HOST-HEADERS.md, screenshots (P9)
app/               Next.js App Router (layout.tsx, page.tsx, globals.css)
components/        UI components, views, charts (SVG)
lib/engine|state/  per §3.3        content/  glossary, linter copy, templates
e2e/               Playwright        public/  favicon, static assets
.github/workflows/ ci.yml            scripts/ spec-coverage.mjs
```

### 8.2 README-vs-CONTEXT split

README = the product's public story: one-liner, thesis paragraph, screenshots, feature tour, quickstart (`npm ci && npm run dev`), stack + architecture in 10 lines, deploy-your-own, docs map, license. Status-honest at every stage (currently: locally verified P0 shell, not deployed). No process/agent material.
CONTEXT (this doc) = everything about *how the work happens*: the plan, decisions, protocols, session log. A README reader should never need this file; an agent should rarely need more than this file.

### 8.3 Versioning & commits

- Semver-per-commit, default message format: **`Commit vX.Y.Z: <short summary>`**. Historical owner-approved titles are intentionally noun-style and are not rewritten. Any suffix or alternate title must be proposed explicitly rather than inferred. Once P0 creates it, the same version also lives in `package.json`, its lockfile, and the app footer.
- Mapping: v0.0.1 = this init; minor bump per completed phase (P0→v0.1.0 … P8→v0.9.0); patches for increments within a phase; **v1.0.0 = P9 sign-off**.
- **Commit protocol (owner-owned, agents never skip):** the agent proposes the exact version number and the exact commit message, lists the staged files, and waits for the owner's explicit go-ahead on both before running `git commit`. The owner may amend either the version or the message before authorizing; the agent respects the amendment verbatim. Trunk-based on `main`; short-lived branches only for risky spikes. `git log` is the changelog (the message format makes it one).
- **Historical v0.0.1 timing:** the owner required and approved author/committer time `2026-07-13 15:00 +05:30`; the completed commit has that timestamp. Do not reuse or alter it.

---

## 9. Decisions ledger

| # | Decision | Options considered | Why |
|---|---|---|---|
| D-01 | **Next.js (App Router, `output: 'export'`) + React + TS strict + Tailwind, static export, Vercel deploy** | Vite+React (prior pick); vanilla ESM | Owner fluency + Vercel's zero-config repo-connect; App Router gives clean file-based structure without SSR while `output: 'export'` keeps the bundle a plain static SPA (no server, no data). §3.1–3.2. Flipped from Vite in S1. |
| D-02 | One primitive: utility upper envelope over offer sets | Per-feature ad-hoc rules; MC-only simulation | Concentrates correctness in one tested function; tiers/free/add-ons/bundling/competitors all unify; closed form → instant UI |
| D-03 | Within-segment heterogeneity analytic (lognormal ε), MC only for assumption uncertainty | MC for everything | Two conceptually different uncertainties; analytic layer is exact, fast, and testable against closed forms; MC reserved for what genuinely needs it |
| D-04 | Lognormal for WTP scale & bands | Normal, triangular, uniform | Positive support, right skew (matches WTP reality), closed-form partial expectations, clean P10/P90 fitting |
| D-05 | Hero = simulation wind tunnel; methods live in Analyze | Methods-led (PSM/conjoint centerpiece) | The live self-selection reveal is the differentiated thesis and the demo moment; methods-led reads as a stats toolkit. Methods feed the model rather than star |
| D-06 | **v1.0 ships the Decision Record; the pricing-page mock is a v1.1 extension** (Markdown + print-CSS PDF) | Defer the record entirely; PNG/PPTX export | The record is the strategy artifact and cannot be cut from v1.0. The mock is valuable but not allowed to delay a correct core; its status now matches §2 and §6. |
| D-07 | **CBC/MNL, MaxDiff-lite, and competitive positioning are v1.1 extensions built to the stated standard; competitor-as-alternative is core engine capability** | Shallow versions; quietly omit methods | Owner directive S1 was depth, not a license to ship a fragile UI before the core. v1.1 retains analytic MNL, a valid WTP contract, MaxDiff, and a segment-scoped map; competitor loss remains a core simulation/linter capability. |
| D-08 | Behavioral effects as cited linter checks, not numeric multipliers | Model decoy uplift quantitatively | Invented uplift coefficients would violate the correctness bar the product stands on; deterministic detectors with honest citations are defensible and more useful. (Disagrees with the brief's hint — deliberately.) |
| D-09 | Metrics: flat + per-seat (seats as segment multiplier); usage-based deferred | Full usage engine in v1 | Per-seat is one multiplication and covers most SaaS; usage needs consumption distributions — a second engine that would dilute v1 correctness focus |
| D-10 | Free tier = price-0 offer in the engine; trials deferred | Model trials | Free falls out of the envelope naturally (and powers leakage analysis); trials are temporal dynamics (v2 with retention) |
| D-11 | PSM is survey-first; model-generated curves are explicitly "illustrative" | Synthetic PSM as a headline feature | Synthetic survey results dressed as evidence = exactly the fake-rigor slop to avoid |
| D-12 | Charts hand-rolled SVG; no chart/router libs; deps capped at 7 | Recharts/visx; react-router | Bespoke viz (dots, waterfall, envelope, PSM crossings, part-worths, positioning map, tornado) has no library anyway; bundle, control, craft. Tabs are state; hash reserved for scenario payload |
| D-13 | **Light + dark themes must both pass the recorded AA audit; palette re-toned per theme; header edits the scenario theme setting** | Light only (prior pick); dark only | Flipped in S1: full theme parity signals design maturity for a portfolio piece, and Next.js + Tailwind make it cheap. The audit is a P9 gate, not a current claim. Zero-network intent remains unchanged (no analytics, self-hosted font). |
| D-14 | v0.0.1 = docs + license + .gitignore only; toolchain lands in P0 | Scaffold now | Keeps the first commit purely reviewable intent; tool versions get chosen fresh at scaffold time, recorded in D-19b |
| D-15 | **Vercel deploy via repo-connect (Next static export)** | GitHub Pages (prior pick), Cloudflare Pages, Netlify | Owner-confirmed S1 given the Next.js pick; Vercel is the first-class path for Next; free tier trivially covers a portfolio SPA; still no lock-in (the built `out/` folder is portable to any static host) |
| D-16 | Ties break to cheaper offer; documented everywhere | Break to higher tier | Conservative revenue reporting; determinism in tests (see T-BND-04 for why it matters) |
| D-17 | First CBC release = pooled MNL with random-balanced design; HB and D-efficient designs are later refinements | Full HB + D-efficient in the first release | Pooled MNL with SEs is enough to propose an aggregate bridge to selected value-matrix cells and enough for the pedagogical arc; HB is a step-change in complexity (per-respondent priors, sampler tuning) that would dilute the correctness focus. D-efficient search is a refinement, not a foundation. Release timing follows D-28/§6. |
| D-18 | Competitor implemented as BOTH a chart point (Positioning map) AND a per-segment offer in the envelope engine | Chart-only competitor entry | Dual role means the same competitor data drives a picture AND a number; the "what % of enterprise leaves for Snowflake at this price?" answer emerges from the same envelope math the tiers use — no second engine, no fake integration |
| D-19 | Production dep budget bumped 6 → 7 to accommodate `next` | Stay at 6 by picking Vite; drop lz-string and store scenarios by base64+JSON | 7 is the minimum honest count for the stack chosen in D-01; the accent isn't dependency-count-as-vanity, it's *no chart libs, no router libs, no state megaframeworks*, which still holds. lz-string earns its slot: URL-hash sharing is a key product feature and JSON is not trivially short |
| D-20 | **Dual tie mode:** the simulator uses `'conservative'` (D-16); the closed-form screening/bundling oracles use `'seller-favorable'` at the σ→0 point path (participate + highest-value on ties) | One global tie rule; perturb fixture prices below binding values; recompute oracle numbers under the lognormal model | Review S2 (verified by simulating the spec) found D-16 alone makes T-SCRN-* return ≈2000 not 8000 and T-BND-01 return 0 not 20·n — the textbook menus price served buyers exactly at binding values, so every served buyer is on a tie. Seller-favorable is the δ→0 limit and recovers the exact textbook numbers; conservative stays the product default. Guard added to §4.2/§4.5/§4.6 so a weak executor can't "fix" the engine to force the wrong number; T-SCRN-01 moved σ=1e-3 → σ=0 (1e-3 drops the below-median half regardless of tie rule) |
| D-21 | Competitive value frontier is the **Pareto min-price-at-value staircase**, not a convex hull | Convex hull under (V, −P) | Review S2: the prose said "convex hull" while the construction algorithm computed the Pareto set — a contradiction that would give a weak model two different frontiers. Competitors are discrete and unmixable, so a Pareto-efficient point interior to the hull must stay on the frontier; the staircase is the correct object. Purged "hull"; T-CMP-01 pinned with a hull-vs-staircase fixture |
| D-22 | MaxDiff normalization gets an explicit **all-equal degenerate branch** → uniform `100/m` across all `m` scored items | Leave the shift-then-normalize formula as-is | Review S2 found the zero-division branch; S4 corrected its denominator from per-task `k` to total-item `m` and added zero-appearance validation. This makes the asserted sum-to-100 invariant well-defined. |
| D-23 | **P7d split** into P7d-1 (estimator) / P7d-2 (views) + a mandatory analytic-vs-finite-difference **derivative gate** before the MNL recovery test | Keep P7d as one L phase; rely on the concavity tests only | Review S2: the analytic MNL gradient/Hessian is the highest-risk engine math for a lower-capability executor, and the recovery test is a global pass/fail that doesn't localize a sign/index bug. The derivative gate is a local correctness signal; the split de-risks sizing and makes the bridge UI a clean cut point |
| D-24 | **Scope posture:** the non-cuttable core (P0–P6a, P7a, P7b, P8 record, P9) is v1.0, built first; research-method extensions (P7c / P7d-1 / P7d-2 / P7e) follow only when the core is green | Treat all phases as equally required, cutting only under schedule pressure | Review S2: the full surface is 2–3 products' worth of work. Core-first sequencing protects correctness. An engine ships with its extension unless the v1.0 core has a named tested dependency on that engine path; unexposed engines are not marketed as shipped. D-28/§6 is the release authority. |
| D-25 | **Correct the value waterfall and price units** | Preserve the earlier formulas and account/seat ambiguity | `OwnBuyerSurplus` excludes competitors, free fenced offers contribute to `FencingGap`, and the identity now holds. WTP is account-level at the segment's seat count; per-seat sweeps use per-seat bounds. |
| D-26 | **Positioning is segment-scoped and discrete; CBC bridge requires identified numeric price** | Interpolated frontier gaps; categorical-price pseudo-WTP | Zero-utility rays have slope `ε`; a discrete competitor map cannot interpolate unavailable offers. MNL is concave only under stated conditions, and a single price coefficient exists only for numeric price. |
| D-27 | **Bound sharing and verify runtime privacy** | Put all survey records in URLs; grep-only network checks | Compact URLs exclude respondent records and have byte/decompression guards; complete transfers use JSON. A browser request audit and deployment CSP substantiate the zero-network claim. |
| D-28 | **v1.0/v1.1 release contract resolves prior scope contradictions** | Keep mandatory-v1 ledger entries alongside optional cut lines | Lower-capability executors now have one source of truth: Decision Record/core first, then reviewed extensions. |
| D-29 | **Renamed Product Studio → Wind Tunnel** (owner, 2026-07-13); repo/folder `product-studio` → `wind-tunnel`, GitHub + local remote updated | Keep the "Product Studio" placeholder | The product's signature interaction is the always-live "wind tunnel" simulation rail (§5.1) — naming the product after its core metaphor is native and owned (mirrors the sibling repo's "Best Response" register). Done pre-P0 while docs-only, before the name is baked into package.json / metadata / OG / first-run copy; §11 item 2 updated. |
| D-30 | **S4 contract hardening: P50-centered uncertainty, adaptive sweeps, pooled CBC bridge, achievable balance, corrected MaxDiff/CMP tests, one competitor-synthesis owner, and explicit v1.1 dependency order** | Preserve internally contradictory contracts and rely on implementation-time interpretation | Each correction follows from a counterexample or missing dependency recorded in §0.4/S4. B-001 was the only remaining economic-policy ambiguity and is now resolved in D-31. |
| D-31 | **Exact own/competitor identity ties are competitor-first** (owner, S10) | Incumbent/own-first or an explicit split | Conservative attribution avoids booking ambiguous revenue as ours and gives the mass a defined home in `CompetitorLoss`; T-CMP-08 makes the result independent of input order. |
| D-32 | **T-STAT-02 uses 1e-8 only at the ±6σ endpoints** (owner, S10) | Preserve an unachievable full-grid 1e-9 target; narrow the grid | Number-returning IEEE-754 Φ loses enough upper-tail detail at +6σ that the best exact round trip is 8.85e-9. The interior target remains 1e-9. |
| D-33 | **P3 uses the four planned state/schema/share packages, exact-pinned** | Custom validation/history/compression or a new dependency beyond the planned budget | `zod@4.4.3`, `zustand@5.0.14`, `zundo@2.3.0`, and `lz-string@1.5.0` complete the planned production set of seven. They provide versioned validation, state/undo, and bounded compressed sharing without broadening the architecture. |
| D-34 | **Client-JS gzip ceiling raised 300 → 320 KiB for v1.1 extension code** (owner, S29) | Hold 300 KiB and cut Conjoint/MaxDiff UX; lazy-load extensions out of the initial chunk and gate only initial-load bytes | The 300 KiB figure (§3.5, itself raised 250→300 in S1 to absorb the Next static-export runtime, D-01) was sized for the **v1.0 core**. P7c + P7d-2 land four additional research surfaces; the honest options were to shrink the surfaces below usefulness or to state a v1.1 ceiling. Measured footprint at P7d-2 close is 303.9 KiB, leaving ~16 KiB of headroom that every future addition must be audited against. §3.5 and P9's acceptance gate are updated to 320 KiB so the code gate and the spec agree; "as small as possible under Next's static-export path" remains the practical target. |
| D-35 | **v1.0 ships the tabbed studio shell; §5.1's always-on desktop wind-tunnel rail is not a v1.0 requirement** | Rebuild the shell as a two-pane desktop layout before v1.0 | The live-consequence promise is already met — Model carries an engine-backed KPI rail, Design carries the live linter dock, and Simulate is one keystroke away — while a layout rewrite at the release gate would put the green 320 px reflow, tri-browser, and axe results at risk for presentation gain only. §5.1 records the shipped arrangement. Revisit as a v1.1 layout increment. |
| D-36 | **Visualization fidelity accepted as shipped: 20 buyer dots at disclosed 5% granularity, fixed Okabe–Ito categorical hex in both themes, one selected tier per price sweep** | 100 dots at 1%; per-theme re-toned palette; small-multiple R(p)+Q(p) per tier | 100 dots × 6 segments would put ~600 transitioning SVG nodes against a 16 ms interaction budget currently met at 4.4–5.6 ms. The fixed Okabe–Ito set already passes axe at zero serious/critical in both themes, which is the substantive requirement behind D-13's re-toning idea. Sweep demand is present in the paired table, satisfying chart/table parity. Granularity is now disclosed in-surface. |
| D-37 | **No `lib/engine/index.ts` barrel; per-module imports remain the engine's public surface** | Create the barrel named in §3.3 | A barrel re-exporting every engine module invites the static-export bundler to pull unused engine code into the initial chunk, against a gzip ceiling with ~12 KiB of headroom. Per-module imports are explicit, and the engine-purity lint rule (hardened in S34) is what actually enforces the boundary. §3.3's structure listing is amended to match. |
| D-38 | **Persisted σ is bounded to ≤ 2 with no schema minimum; MaxDiff items are capped at 12 to match §4.10** | Enforce §4.12's 0.05 UI floor in the schema too; raise §4.10's cap to the shipped 20 | σ = 0 is a legitimate deterministic engine path that the §4.5/§4.6 oracles and several fixtures depend on, and the schema is shared with them, so the 0.05 floor stays an authoring guard in the Model UI. The MaxDiff cap moves code to spec because §4.10 is normative and no shipped fixture exceeds 12. |

**D-19b (Phase 0, S8; P3 amended):** all toolchain versions are exact in `package.json` and `package-lock.json`: Node `24.18.0` (`.nvmrc`); `next@16.2.10`, `react@19.2.7`, `react-dom@19.2.7`; P3's `lz-string@1.5.0`, `zod@4.4.3`, `zundo@2.3.0`, `zustand@5.0.14`; `tailwindcss@4.3.2`, `@tailwindcss/postcss@4.3.2`, `postcss@8.5.19`; `typescript@5.9.3`; `eslint@9.39.5`, `eslint-config-next@16.2.10`, `prettier@3.9.5`; `vitest@4.1.10`, `fast-check@4.9.0`, `@testing-library/react@16.3.2`, `@testing-library/jest-dom@6.9.1`, `jsdom@29.1.1`; `@playwright/test@1.61.1`, `@axe-core/playwright@4.12.1`; and the matching Node/React types. TypeScript 7 and ESLint 10 were current but incompatible with Next's pinned ESLint integration, so the latest compatible releases were selected. A locked `postcss@8.5.19` override replaces Next's vulnerable nested `8.4.31`; both production-only and full `npm audit` then report 0 vulnerabilities. Pin exact; do not add carets.

## 10. Decisions I'm making for you — veto before build

V-01 **Stack: Next.js (App Router, `output: 'export'`) + React + TS + Tailwind, client-only static, Vercel deploy** (D-01, D-15). *S1: confirmed by owner.*
V-02 Hero framing: wind tunnel leads; PSM/Conjoint/Bundling live in Analyze — Research; competitive map in Analyze — Positioning (D-05).
V-03 Roster: **Conjoint (with a valid numeric-price bridge), MaxDiff-lite, and the competitive map are reviewed v1.1 extensions; competitor-as-alternative is core** (D-07). *S1 depth directive preserved.*
V-04 Behavioral effects: linter with citations, **no** numeric decoy multipliers — this overrides the brief's "model behavioral effects" hint on correctness grounds (D-08).
V-05 **Communicate in v1.0:** Decision Record, Markdown + print-PDF. The pricing-page mock follows as a reviewed v1.1 extension (D-06).
V-06 Templates in v1: exactly 3, data-only presets (§2.1).
V-07 Metrics: flat + per-seat; usage-based, trials, annual-term discounts deferred (D-09, D-10).
V-08 Buyer model: lognormal ε within segments (analytic), MC only for assumption bands (D-03, D-04).
V-09 **Light + dark themes must both pass the P9 audit**; zero analytics/telemetry, self-hosted font (D-13). *S1: dark mode added to the plan.*
V-10 Dependency cap: **7** production deps as listed (§3.4, D-19).
V-11 **Commit protocol**: agent stops and proposes exact version + message; owner must approve both before `git commit` runs (§0 rule 4, §8.3). *S1: owner-stated.*

Silence = consent for any item; an overruled item gets a ledger entry and plan patch before P0 starts.

## 11. Open questions & owner actions

1. ~~**Repo:**~~ **Resolved:** `origin` is configured and `main` tracks `origin/main`. **Deploy:** owner still connects the existing repository to Vercel — needed for P0's live URL; CI/config can land first.
2. ~~**Product name.**~~ → **Resolved: renamed to "Wind Tunnel" (v0.0.3, S3, ledger D-29).** The S1 placeholder "Product Studio" is retired; the name is the product's core live-simulation metaphor (§5.1). First-run copy and P0 metadata use "Wind Tunnel".
3. ~~**Veto pass on §10**, then explicit go-ahead to commit v0.0.1.~~ → **Resolved S1c:** owner gave the go-ahead 2026-07-12; version `v0.0.1` and message `Commit v0.0.1: Init + Readme + Context` used as proposed. (§10 items remain overridable until P0 starts.)
4. ~~**v0.0.1 commit timestamp.**~~ → **Resolved S1c:** owner requested `2026-07-13 15:00 +05:30`; the commit was created with that author/committer time. No scheduling machinery remains.
5. ~~**Exact own/competitor tie policy (B-001).**~~ **Resolved S10:** competitor-first attribution, recorded in D-31 and pinned by T-CMP-08.
6. ~~**T-STAT-02 endpoint policy (B-009).**~~ **Resolved S10:** retain `1e-9` in the interior and use `1e-8` at the ±6σ endpoints, recorded in D-32.
7. FYI: after S10, `origin/main` remains at `da5ba98` (v0.0.4) while local `main` contains the unpushed v0.1.0…v0.1.7 P0 and v0.2.0…v0.2.4 P1 series. No deployment or remote CI execution occurred; a Vercel connection remains required for P0's public shell.

## 12. Backlog (v1.1 / v2 parking lot)

Usage-based metric engine · hierarchical Bayes for Conjoint (per-respondent part-worths) · D-efficient design generation for Conjoint · MNL best-worst estimation for MaxDiff · annual/term discounts & trial dynamics (retention model) · joint price optimizer with honesty framing · elasticity + cross-tier substitution matrix readout (analytic derivatives of §4.2 shares) · non-additive feature values (complements/substitutes) · segment-to-competitor value mapping via a survey shortcut · currency/i18n · PNG/slide exports · scenario version history & diffing · in-app worked tutorial ("defend this decision" walkthrough).

## 13. Session log

**S34 — 2026-07-27 (full audit, decision pass, and final handoff; committed as v0.17.0…v0.17.4).** Owner-commissioned thorough pre-v1 audit of the completed §6 build, consolidating and extending two earlier passes whose fixes had not been applied. Fixed and pinned B-010…B-025 (see §0.4), and additionally: made §4.12 a required spec-coverage section with three citing tests, aligned the MaxDiff cap to §4.10, replaced the positioning map's diagonal connector with an honest step-style staircase, disclosed buyer-dot granularity, extended and attached the glossary (8 new §5.2 terms), added the missing E2E-04 browser flow and labeled every flow with its §6 ID, added an app-level error boundary and an exact compact-link payload preview, removed the unused root live region, relaxed the accidental exact `engines.npm` pin, clamped the allocation residual, gave linter E5 a signed direction, and corrected README's P0 status row. Ledger D-35…D-38 record the four accept-as-shipped calls (tabbed shell, visualization fidelity, no engine barrel, σ/MaxDiff bounds); CONTEXT §5.1/§5.2/§3.3/§4.6/§4.10 now describe shipped, test-pinned behavior. The committed audit series is `v0.17.0` Shell Recovery + Model Conventions (`/1-F5-T6`, 00:15), `v0.17.1` Research Import Safety + Limits (`/2-F5-T6`, 00:30), `v0.17.2` Linter + Positioning Integrity (`/3-F5-T6`, 00:45), `v0.17.3` Share + Decision Record Clarity (`/4-F5-T6`, 01:00), and this `v0.17.4` Spec Coverage + Release Handoff (`/5-F5-T6`, 01:15), all post-dated 2026-07-27 +05:30. Final local verification: typecheck, ESLint, Prettier, privacy scan, static build, **133 unit tests**, spec coverage (§4.1–§4.12), and tri-browser E2E **44 passed / 4 skipped / 0 failed**. The completed audit report was removed. What remains for v1.0.0 is owner-only: Vercel connection, a remote CI run, the manual assistive-technology matrix, the URL → decision-record user test, README screenshots, and the MODEL-SPEC read-through.

**S0 — 2026-07-12 (Fable, planning).** Repo initialized (`main`). Authored CONTEXT.md, README.md, LICENSE (MIT, Sidakpreet Singh), .gitignore. Staged for `Commit v0.0.1: Init + Readme + Context` — **not committed** (awaiting owner preview + explicit ask). Next: owner veto pass (§10), answers to §11, then commit and start P0.

**S1 — 2026-07-12 (Fable, revising per owner).** Owner delivered directives on the initial plan (via chat, this session):
- **Stack:** switch to Next.js (App Router, `output: 'export'`) + Vercel deploy → D-01 and D-15 flipped; §3 and P0 rewritten; dep budget bumped 6→7 (D-19); bundle gate raised 250→300 KB gz to account for Next runtime overhead (still hand-audited at P0).
- **Roster:** D-07 flipped — Conjoint (CBC) with proper MNL estimation, MaxDiff-lite, and Competitive price-value map are **in v1, done properly**. Added §4.10 Conjoint/MaxDiff-lite (MNL log-likelihood, Newton-Raphson, effects coding, price-bridge to WTP with significance gate) and §4.11 Competitive map & competitor-as-alternative (frontier hull, iso-utility rays, competitor-offer synthesis into the envelope engine with a new `CompetitorLoss` term in the conservation identity). Added phases P7d Conjoint/MaxDiff and P7e Positioning; new tests T-CNJ-01…06, T-MXD-01…02, T-CMP-01…06; added linter rule E7 (competitor loss); renumbered §4.10→§4.12.
- **Communicate:** D-06 reinforced — not deferred, no cuts.
- **Dark mode:** D-13 flipped — light + dark, both AA-audited; theme is a scenario setting with header override; palette re-toned per theme.
- **Product name:** kept "Product Studio" for now (§11 item 2 resolved).
- **Commit protocol:** owner formalized — agent must stop and propose exact version + commit message before every commit, owner approves both. Encoded in §0 rule 4, §8.3, and new veto V-11.
- **v0.0.1 timestamp:** owner asked for commit dated 2026-07-13 15:00 local (§8.3, §11 item 4) — handled by post-dating the commit with git date env vars at whatever time the go-ahead arrives.

Files updated: CONTEXT.md (this revision), README.md (stack + roadmap patch). All staged, **still not committed** (per §0 rule 4 and the timestamp ask).

**S1b — 2026-07-12 (Fable, continuation).** S1's session was interrupted mid-README patch; a fresh session resumed from this doc and completed the remainder: README stack line (Next.js static export, Vercel, light + dark) and P7 roadmap row (now lists Conjoint/MaxDiff + competitive positioning); .gitignore rewritten for Next.js (`.next/`, `out/`, `next-env.d.ts`, `*.tsbuildinfo`, `.vercel`). Owner simplified the timestamp mechanism: no scheduled reminder — just post-date the commit via git date env vars when the go-ahead arrives (§8.3 and §11.4 updated; the briefly-created scheduled task was deleted). All four files re-staged; **still not committed**, awaiting the owner's explicit approval of version + message.

**S1c — 2026-07-12 (Fable).** Owner asked for a pickup-completeness check, then gave the conditional go. Reverify pass clean: index = worktree (no drift), LICENSE verified (MIT, Sidakpreet Singh), no stale stack references outside intentional ledger records. `Commit v0.0.1: Init + Readme + Context` executed, post-dated `2026-07-13 15:00 +05:30` per §8.3 — repo history begins. Nothing pushed (no remote). Next: owner creates GitHub repo + Vercel connection (§11.1); P0 scaffold → v0.1.0 on the next explicit ask.

**S2 — 2026-07-13 (Opus 4.8, fresh-eyes structural review).** Owner commissioned an all-angles soundness review of the plan before execution (sibling repo gt-sandbox was reviewed in the same pass and cleared on core correctness — its six game oracles recompute exactly and its 2×2 classifier is provably complete). Findings here were verified by direct recomputation, not judgment: the game-theory oracles by exact rationals, and the PS economic engine by simulating it against its own §4 spec. One critical defect plus several smaller fixes, all applied to this doc:
- **Tie-rule / oracle contradiction (critical, D-20).** Simulating the engine under D-16 ("break to cheaper / outside") returns screening revenue ≈2000 (σ=0) or 5000 (σ=1e-3), never 8000; a pure bundle priced at value returns 0 (σ=0) or 10·n, never 20·n. Root cause: the textbook menus price served buyers at exact binding values (all ties), and D-16 sends the indifferent buyer to the outside option. Fix: a dual tie mode — `'seller-favorable'` (participate + highest-value on ties, σ→0) for the closed-form oracles, `'conservative'` staying the simulator default — plus guard notes in §4.2/§4.5/§4.6 so the engine is not "fixed" to force the number, and T-SCRN-01 moved σ=1e-3 → σ=0. Verified: the corrected convention reproduces T-SCRN-01…04 (8000 / 6000 / 5000>4400) and T-BND-01 (20·n) exactly.
- **Competitive frontier hull-vs-staircase (D-21):** purged "convex hull," pinned the Pareto staircase.
- **MaxDiff divide-by-zero (D-22):** added the all-equal degenerate branch → uniform 100/k.
- **Conjoint hardening (D-23):** split P7d, added a mandatory analytic-vs-finite-difference derivative gate before the recovery test.
- **Scope posture (D-24):** core-first v1.0; research-method view layers optional-by-default.
- **Minor:** reconciled the §3.2 "sub-10 ms" vs §3.5 "<150 ms" MC figures (raw compute vs end-to-end ceiling); removed the stale "vs. Vite budget" phrasing in §3.5; made P1's competitor-offer synthesis functional (not a stub) so E7 is genuinely testable in P5.
Ledger D-20…D-24 added; §3.5, §4.2, §4.5, §4.6, §4.10, §4.11, §4.12, §6 (P1, P5, P7d→P7d-1/-2, scope posture) revised; README status bumped to v0.0.2. Version → v0.0.2. Owner authorized `Commit v0.0.2: Key Implementation Fixes /O8`, post-dated 2026-07-13 16:00 (+05:30) — one hour after v0.0.1. Nothing pushed (no remote yet, §11.1).

**S2b — 2026-07-13 (fresh live-repo re-review).** The prior S2 fixes remain valid, but a fresh audit found additional genuine contract defects that would make a faithful implementation wrong: the §4.3 identity double-counted competitor utility and omitted free-tier fencing; per-seat value/sweep units were ambiguous; the Positioning map used the reciprocal ray slope and interpolated discrete competitors; the CBC bridge used an undefined categorical `β_price` and assumed strict concavity; URLs could silently exceed practical limits with respondent records; and v1.0/v1.1 scope statements conflicted. Applied D-25…D-28: corrected the formulas and tests, made Positioning segment-scoped/discrete, hardened CBC estimator/bridge statuses, bounded compact sharing and added runtime request verification, and made the release contract authoritative. Updated the stale remote/deploy handoff facts. This amendment is folded into `Commit v0.0.2: Key Implementation Fixes /O8` at the same post-dated time; no application code exists or was changed.

**S3 — 2026-07-13 (Opus 4.8, rename).** Owner finalized the product name: **Product Studio → Wind Tunnel** (ledger D-29) — the name is the product's core live-simulation metaphor (the always-on wind-tunnel rail, §5.1). Executed the full rename pre-P0 while the repo is docs-only: GitHub repo `product-studio` → `wind-tunnel` (via `gh repo rename`; local remote URL auto-updated to `…/wind-tunnel.git` and re-fetched clean), local folder `product-studio` → `wind-tunnel`, and the name updated in README.md (title, self-reference, status version) and CONTEXT.md (project header, §11 item 2, this ledger/log). The historical S1 line recording the retired "Product Studio" placeholder is left intact (append-only session log). Version → v0.0.3. Owner authorized `Commit v0.0.3: Wind Tunnel`, post-dated 2026-07-13 17:00 (+05:30) — one hour after v0.0.2. Not pushed (owner did not request a push).

**S4 — 2026-07-13 (Codex, fresh-eyes audit and handoff bootstrap; uncommitted).** Started from a clean tree on `main` at `a7100ee`; live remote verification showed GitHub `main` at the same commit, so the three existing commits are now pushed even though S3 correctly recorded that its commit was not pushed at that session's end. No actor/time for the later push is inferred. Audited every tracked file, all three commits, timestamps, tracking/remotes, ignored status, version policy, release scope, math/test contracts, accessibility gates, and README claims. There is no application/package/test/deployment to execute yet. Corrected the unambiguous documentation/spec defects recorded as B-002…B-008 and D-30; front-loaded the current handoff and strengthened the pre-commit/update protocol. Left B-001 open because identical own/competitor attribution is an economic-policy choice, not a fact the audit can infer. Files changed: `README.md`, `CONTEXT.md`; unstaged, not committed, not pushed. Checks passed: `git diff --check`, unchanged index, local Markdown links, heading/reference inventory, and stale-contract scan. No package/toolchain exists, so there are no formatter, build, lint, or test commands to run.

**S5 — 2026-07-15 (Fable 5, owner-requested cross-check of the S4 audit; uncommitted).** Independently re-verified every S4 claim against HEAD `a7100ee`, the live remote (still in sync), and the working diff. Verdict: B-001 is a genuine open policy gap, and the B-002…B-008 corrections are genuine and technically sound. Fixed four S4 slips, all in CONTEXT.md: (1) P2's acceptance list now includes the new T-SWP-04; (2) P9's task line reverts to "perf budgets re-verified" and "release checklist" — cold-cache Web Vitals gates and the rollback runbook are §0.5 optional ideas the owner has not approved, and no Web Vitals budgets exist in §3.5/§7 to "re-verify" (the P9 manual accessibility matrix stays: D-13's owner-decided "audited AA" claim needs more than axe to substantiate); (3) §2.1's Model row now shows the segment-size P10/P90 band, matching §4.8 and P4; (4) T-CMP-08 now pins "attributed shares (P1) or KPIs (P2+)" so P1's gate is runnable before §4.3 lands. Historical S1/S2 log entries retaining superseded values (100/k, T-CMP-01…06) were correctly left as history. README.md needed no correction. Files changed: CONTEXT.md only; everything remains unstaged, uncommitted, unpushed, awaiting owner review.

**S6 — 2026-07-15 (Fable 5, pre-build finalization; committed as v0.0.4).** Owner reviewed S4+S5 and closed the loop: the two S5 reverts stand (cold-cache Web Vitals gates and the rollback runbook remain §0.5 optional ideas — decision calls happen at build time), no further review items were open, and the plan is **declared final for the v1 build**. Role split confirmed by the owner: planning/management stays with the planning agent; implementation is delegated to a separate executing agent working from this doc under §0.6/§6/§7. Updated §0 and §11 to the post-commit state, bumped README status to v0.0.4 (plan finalized, pre-build), and committed both files as owner-authorized `Commit v0.0.4: Pre-Build Check Complete /S6-F5`, post-dated 2026-07-18 02:00 (+05:30), author ItsMonarch04. Not pushed (owner: no remote pushes yet); `origin/main` remains at v0.0.3 `a7100ee`. Next: P0 scaffold on the owner's go; B-001 own/competitor tie attribution is the one owner decision gating P1.

**S7 — 2026-07-15 (Codex, P0 scaffold; uncommitted).** Owner explicitly started the build. Manually scaffolded the non-empty repository with a strict, static-export Next.js application: P0 shell (header, tab skeleton, seed chip, version footer, light/dark tokens), local Inter font with license, Tailwind, TypeScript, ESLint/Prettier, Vitest/Testing Library, Playwright + axe, GitHub Actions, Vercel/static-host headers, privacy and bundle gates, and a `spec-coverage` P0 stub. The runtime remains entirely client-side; the only production packages currently installed are Next, React, and React DOM, leaving the planned production-dependency budget intact for later phases. Toolchain pins are recorded in D-19b. Current TypeScript 7 and ESLint 10 were incompatible with Next's lint integration, so the latest compatible TypeScript 5.9.3 and ESLint 9.39.5 were pinned; the initial audit found Next's nested PostCSS 8.4.31 vulnerable, then a compatible locked PostCSS 8.5.19 override produced 0 production and full-tree audit findings. Local checks passed: `typecheck`, lint, format check, 2 Vitest tests, privacy scan, static build, 183.2 KiB gzip bundle gate (≤300 KiB), and Chromium Playwright request/axe audit (0 serious/critical findings). The engine-purity rule was proven with a temporary forbidden UI import that failed lint, then the fixture was deleted. No commit, push, GitHub Actions run, or Vercel deployment occurred. P0 stays open pending a test-PR CI result and the owner’s Vercel connection/public shell check; B-001 still gates P1.

**S8 — 2026-07-16 (Codex, P0 commit series; committed locally).** The owner requested an eight-commit split with fair file distribution, mandatory release-version alignment, and post-dated local history. Created the complete P0 series without a push: `v0.1.0` Static Runtime + Toolchain (`/1-F5-T6`, 2026-07-16 16:00 IST); `v0.1.1` Application Shell + Font (`/2-F5-T6`, 16:10); `v0.1.2` Theme + Code Quality (`/3-F5-T6`, 16:20); `v0.1.3` Unit Testing + Components (`/4-F5-T6`, 16:30); `v0.1.4` Browser Testing + Accessibility (`/5-F5-T6`, 2026-07-19 04:00); `v0.1.5` Privacy + Build Gates (`/6-F5-T6`, 04:10); `v0.1.6` Static Host + Security (`/7-F5-T6`, 04:20); and this `v0.1.7` CI + P0 Handoff (`/8-F5-T6`, 04:30). Every commit advances only X in the suffix, aligns README/package/lockfile versions, and uses the same author/committer time. Final verification remains the S7 local evidence: typecheck, lint, format check, Vitest, privacy scan, static build/bundle gate, Chromium axe/request audit, and zero-vulnerability audit; CI and Vercel remain intentionally unverified external gates. P0 is therefore locally verified, committed, and ready for a clean next-agent pickup, not declared complete.

**S9 — 2026-07-16 (Codex, P1 core; uncommitted).** Began P1 from the clean P0 series. Added pure `lib/engine` contracts, high-accuracy lognormal statistics/band fitting/partial expectations, joint tier × add-on expansion with effective per-seat pricing, functional competitor-offer synthesis, and the upper-envelope selector with conservative and seller-favorable tie modes. Equivalent own/competitor `(value, price)` pairs deliberately throw `UnresolvedOwnershipTieError` instead of silently attributing a share. Extracted §4 to `docs/MODEL-SPEC.md`, added `@spec`-based coverage reporting to CI, and added T-STAT-01…04, T-ENV-01…05, T-ADD-01 plus the required dual-mode and unresolved-tie guards. Local checks green: format, typecheck, lint, 14 Vitest tests, spec coverage (§§4.1–4.2), static privacy/build/bundle gate, Chromium e2e, and `git diff --check`; the browser test needed the usual approved local localhost bind. P1 remains open: B-001 needs the owner policy/T-CMP-08, while B-009 records that binary64 cannot meet the existing 1e-9 Φ⁻¹(Φ(x)) bound at exactly +6σ (measured 8.85e-9). No commit, staging, push, remote CI, or deployment occurred.

**S10 — 2026-07-16 (Codex, owner decisions and P1 commit series).** Owner resolved the two P1 review points: exact own/competitor identity ties are **competitor-first** (D-31), and T-STAT-02 keeps its 1e-9 interior target with a **1e-8 endpoint tolerance** at ±6σ (D-32). Replaced the unresolved-tie guard with T-CMP-08, which verifies competitor attribution and input-order independence. Owner then authorized the reviewed five-commit series, created locally without a push: `v0.2.0` Engine Contracts + Statistics (`/1-F5-T6`, 2026-07-20 00:30 IST); `v0.2.1` Offer Expansion + Tests (`/2-F5-T6`, 00:40); `v0.2.2` Envelope Selection + Tie Policy (`/3-F5-T6`, 00:50); `v0.2.3` Model Spec + Coverage Reporter (`/4-F5-T6`, 01:00); and this `v0.2.4` CI Coverage + P1 Handoff (`/5-F5-T6`, 01:10). Every commit aligns README/package/lockfile version fields; all P1 local checks are green. No push, remote CI, or deployment occurred.

**S11 — 2026-07-16 (Codex, P2 economics; uncommitted).** Implemented `lib/engine/economics.ts` and its shared contracts: deterministic segment/scenario KPIs, the four-/five-term own-catalog waterfall, conservation residual, explicit removal counterfactual directions, and adaptive 400-point tier sweeps that re-expand affected add-on composites. Added T-ECON-01…06, T-SWP-01…04, and T-SCRN-01…04 (14 new tests); a σ=0 adversarial tie fixture verified the economics layer must consume the selector's direct point-mass winner rather than only the geometric envelope intervals. `spec-coverage` now makes §§4.1–4.5 required. Local gates passed: format, typecheck, lint, 28 Vitest tests, spec coverage, privacy/static build/bundle (183.2 KiB gzip ≤300 KiB), and Chromium browser smoke/axe/request audit. No new decision or dependency was introduced; README and this handoff accurately label P2 as locally complete but uncommitted. No files were staged, committed, pushed, or sent to remote CI/Vercel.

**S12 — 2026-07-16 (Codex, P2 commit series).** Owner reviewed the completed P2 work without release-blocking findings, approved the three-commit split, and required the `/X-F5-S6` suffix plus 2026-07-20 10:00 IST post-dating at 15-minute intervals. Created locally without a push: `v0.3.0` Economics Contracts + Simulation (`/1-F5-S6`, 10:00); `v0.3.1` Economics Tests + Screening Oracles (`/2-F5-S6`, 10:15); and this `v0.3.2` P2 Coverage + Documentation (`/3-F5-S6`, 10:30). Each commit aligns `package.json`, both root `package-lock.json` versions, and README's Version Control block; the final commit also makes §§4.1–4.5 mandatory in spec coverage and records the clean P2 pickup state. P2's local format/typecheck/lint/test/spec-coverage/build/browser gates remain green. No push, remote CI, or deployment occurred.

**S13 — 2026-07-16 (Codex, P3 scenario foundation; uncommitted).** Implemented the versioned `schemaVersion: 1` scenario contract in `lib/state/` without leaking engine types: P50-centred bands, model/design/competitor/settings state, provenance, research-artifact slots, migration fail-closed stub, canonical full JSON import/export (2 MiB pre-parse cap), and separate compact share payloads. Added 8 KiB encoded and 64 KiB decompressed URL guards with actionable JSON-export fallback errors; compact payloads exclude research by schema, while full JSON retains it. Added a Zustand + zundo store, 300 ms debounced localStorage autosave, undo/redo, three validated archetype fixtures, and a minimal Share tab that exposes import/export/link behavior and theme/seed state. Installed the planned exact-pinned production dependencies `lz-string@1.5.0`, `zod@4.4.3`, `zundo@2.3.0`, and `zustand@5.0.14`, bringing the production budget to its planned 7/7 (D-33); install audit reported 0 vulnerabilities. Added T-SCH-01/02 and T-URL-01/02 plus template, cross-field, autosave, undo/redo, and Share UI validation coverage. Local gates pass: Prettier, TypeScript, ESLint, 37 Vitest tests, spec coverage, privacy scan (22 source files), static build, 183.0 KiB gzip bundle (≤300 KiB), `git diff --check`, and Chromium axe/request audit (1/1). `npm ci --dry-run` also accepts the lockfile; this desktop runner is Node 20.20.2, so npm emits the expected engine warning against the repository's pinned Node 24.18.0. No files were staged, committed, pushed, or sent to remote CI/Vercel. P3 is ready for review; suggested release is `v0.4.0`, but commit details remain owner-owned.

**S14 — 2026-07-16 (Codex, P3 commit series).** Owner reviewed and authorized a fair six-commit P3 split, post-dated 2026-07-21 at 15-minute intervals, without a push: `v0.4.0` Scenario Contracts + Dependencies (`/1-F5-T6`, 01:30); `v0.4.1` Scenario Codec + PLG Template (`/2-F5-T6`, 01:45); `v0.4.2` Scenario Store + API Template (`/3-F5-T6`, 02:00); `v0.4.3` Scenario Fixtures + Validation (`/4-F5-T6`, 02:15); `v0.4.4` Scenario-Aware Shell + Persistence (`/5-F5-T6`, 02:30); and this `v0.4.5` Share Controls + P3 Handoff (`/6-F5-T6`, 02:45). Every commit advances the patch version and aligns `package.json`, both root `package-lock.json` versions, README's status/roadmap/Version Control block, and the footer through the package version. The final commit adds the user-facing compact-link and JSON-sharing controls plus this complete clean-pickup handoff. P3's local format/typecheck/lint/test/spec-coverage/build/browser gates remain green; no push, remote CI, or Vercel deployment occurred.

**S15 — 2026-07-16 (Codex, P4 model surface; uncommitted).** Completed the P4 Model surface without adding dependencies: first visits choose one of the three validated archetypes or start blank; segment cards now edit P50-centred market-size and account-WTP bands, seat count, within-segment spread presets/custom σ, and provenance. The value matrix edits either allocation shares (with exact rebalance) or direct account-month feature values (which recompute P50 while preserving log-space band width); arrow keys move between matrix cells. Added initial glossary popovers and an engine-backed live KPI rail, wired entirely through the pure P1/P2 simulation path. Pure editing tests cover endpoint mirroring, deterministic bands, invalid bands, allocation conservation, direct-value total recalculation, and the inline-band UI path; browser coverage proves template → WTP edit → KPI update, keyboard matrix navigation, theme persistence after reload, runtime request privacy, and zero serious/critical axe findings in both themes. The expanded light-theme axe run exposed insufficient token contrast, corrected by darkening the light muted/amber tokens. Local gates pass: Prettier, typecheck, ESLint, 42 Vitest tests, format check, privacy scan (27 files), static build, 261.8 KiB gzip client JS (≤300 KiB), `git diff --check`, and Chromium e2e (1/1). P0’s remote CI/Vercel gates remain open. No files were staged, committed, pushed, or sent to GitHub/Vercel; P4 awaits owner review plus explicit release-version and commit-message direction.

**S16 — 2026-07-16 (Codex, P4 commit series).** Owner approved the reviewed four-commit P4 split, post-dated 2026-07-23 at 15-minute intervals, without a push: `v0.5.0` Model Editing + Contract Tests (`/1-F5-T6`, 12:00); `v0.5.1` Model Workbench + Glossary (`/2-F5-T6`, 12:15); `v0.5.2` Model Shell + Component Tests (`/3-F5-T6`, 12:30); and this `v0.5.3` Browser Audit + P4 Handoff (`/4-F5-T6`, 12:45). Every commit advances the patch version and aligns `package.json`, both root `package-lock.json` versions, README's status/roadmap/Version Control block, and the footer through the package version. The final commit contains the light-theme contrast correction, browser acceptance audit, and this clean-pickup handoff. P4's local format/typecheck/lint/test/spec-coverage/build/browser gates remain green; no push, remote CI, or Vercel deployment occurred.

**S17 — 2026-07-16 (Codex, P5 design surface + linter; implemented).** Implemented P5 without new runtime dependencies. Added a scenario-to-engine adapter, deterministic P5 linter with E1–E7/B1–B2 findings, cited explainers, and unit fixtures that pin every documented trigger, including engine-backed free-tier/add-on counterfactuals and the competitor-loss path. Added the Design workbench: tier and metric cards, feature-fence grid, free-tier control, add-ons, alternate-design creation/duplication/rename, and a decision-useful findings dock. A new blank-model Chromium journey creates three features and two segments, builds a nested three-tier menu, observes linter findings, then clears them; the browser audit also caught and corrected an invalid ARIA list structure in the alternatives switcher. No new ledger decision or dependency was introduced. Local gates pass: Prettier, TypeScript, ESLint, 55 Vitest tests, spec coverage (including 10 §4.9 citations), privacy scan (36 source files), static build, 267.8 KiB gzip client JS (≤300 KiB), `git diff --check`, and Chromium e2e (2/2) with zero serious/critical axe findings. Sandbox execution could not bind the localhost server or complete the production build, so the final build/browser checks were rerun in the approved local environment. P0’s remote CI/Vercel gates remain open.

**S18 — 2026-07-16 (Codex, P5 commit series).** Owner reviewed and authorized a balanced five-commit P5 split, post-dated 2026-07-23 at 15-minute intervals, without a push: `v0.6.0` Design Editing + State Tests (`/1-F5-T6`, 18:00); `v0.6.1` Design Linter + Economics Adapter (`/2-F5-T6`, 18:15); `v0.6.2` Linter Tests + Scenario Integration (`/3-F5-T6`, 18:30); `v0.6.3` Design Workbench + Shell Tests (`/4-F5-T6`, 18:45); and this `v0.6.4` Browser Audit + P5 Handoff (`/5-F5-T6`, 19:00). Every commit advances the patch version and aligns `package.json`, both root `package-lock.json` versions, README's status/roadmap/Version Control block, and the footer through the package version. The final commit adds the browser acceptance flow and this clean-pickup handoff. P5's local format/typecheck/lint/test/spec-coverage/build/browser gates remain green; no push, remote CI, or Vercel deployment occurred.

**S19 — 2026-07-17 (Codex, P6a wind-tunnel reveal; uncommitted).** Implemented P6a in the uncommitted `v0.7.0` worktree without new dependencies. The Simulate tab now derives all display values from the existing scenario-to-engine boundary: a live KPI header with a P7e competitor-loss placeholder, per-segment buyer-dot sorting with a reduced-motion-safe transition, a four-/five-term conservation waterfall, and selectable 400-point per-tier residual-price sweeps. Each visual has a matching semantic table; a debounced polite live region announces result changes. The expensive sweep deliberately starts after the immediate result update, so the central reveal is not delayed. Added component and Chromium coverage for price-driven KPI/dot changes, two chart/table parity spots, reduced motion, and a CDP `TaskDuration` budget across the three warmed templates (4.4–5.6 ms, under 16 ms). Local gates pass: Prettier, TypeScript, ESLint, 56 Vitest tests, privacy scan (37 source files), static build, 272.8 KiB gzip client JS (≤300 KiB), `git diff --check`, and four Chromium flows with zero serious/critical axe findings, including the new Simulate flow. P0’s remote CI/Vercel gates remain open. No files were staged, committed, pushed, or sent to GitHub/Vercel. Suggested next commit target is `v0.7.0`; exact commit series, message, suffix, and timestamp remain owner-owned.

**S20 — 2026-07-24 (Codex, P6a commit series).** Owner approved a four-commit local-only P6a series, post-dated at 15-minute intervals: `v0.7.0` Sweep Adapter + Simulation State (`/1-F5-T6`, 02:00); `v0.7.1` Wind Tunnel Reveal + Shell (`/2-F5-T6`, 02:15); `v0.7.2` Simulation Acceptance + Performance (`/3-F5-T6`, 02:30); and this `v0.7.3` Browser Audit + P6a Handoff (`/4-F5-T6`, 02:45). Every commit aligns `package.json`, both root `package-lock.json` versions, README's status/roadmap/Version Control block, and the footer through the package version; this final commit records the clean pickup state. Fresh local gates pass: Prettier, TypeScript, ESLint, 56 Vitest tests, spec coverage, privacy scan (37 source files), static build, 272.8 KiB gzip client JS (≤300 KiB), `git diff --check`, and four Chromium flows with zero serious/critical axe findings. Playwright required the approved local localhost bind after the sandbox blocked 127.0.0.1:4173; all four flows passed there. No push, remote CI, or deployment occurred. Next implementation phase: P7a — Monte Carlo + tornado.

**S21 — 2026-07-24 (Codex, P7a commit series).** Owner approved a four-commit local-only P7a series, post-dated at 15-minute intervals: `v0.8.0` Monte Carlo Contracts + Engine (`/1-F5-T6`, 12:00); `v0.8.1` Uncertainty Scenario Adapter (`/2-F5-T6`, 12:15); `v0.8.2` Analyze Uncertainty Workbench (`/3-F5-T6`, 12:30); and this `v0.8.3` Uncertainty Acceptance + P7a Handoff (`/4-F5-T6`, 12:45). The phase introduced no dependencies. `lib/engine/montecarlo.ts` uses an injected seeded mulberry32 PRNG to sample only each segment's P10/P90 size and WTP bands, preserves zero-width bands exactly, returns empirical P10/P50/P90 and mean MRR distributions, applies common random numbers to every design, reports paired wins/ties, and calculates sorted one-at-a-time tornado drivers from the active design's P50 baseline. The state adapter transforms sampled values through the existing closed-form scenario simulation path, keeping σ, prices, and fences fixed. Analyze exposes the seed and a 200–5,000 draw selector, active and per-design MRR bands, paired comparison cards, an accessible hand-rolled tornado with matching table, and provenance-led validation to-dos; copy explicitly distinguishes percentiles from confidence intervals. T-MC-01…05, the adapter integration test, UI coverage, and E2E-05 cover the phase. Every commit aligns `package.json`, both root `package-lock.json` versions, README's Version Control block, and the footer through the package version; this final commit also records the complete handoff. Fresh local checks pass: Prettier, TypeScript, ESLint, 63 Vitest tests, spec coverage (requiring §4.8), privacy scan (41 files), static build, 277.3 KiB gzip client JS (≤300 KiB), `git diff --check`, and five Chromium flows with zero serious/critical axe findings. No push, remote CI, or deployment occurred. Next implementation phase: P7b — Van Westendorp.

**S22 — 2026-07-17 (Codex, P7b Van Westendorp; uncommitted).** Implemented the survey-first PSM phase without adding dependencies. `lib/engine/vanwest.ts` is a pure deterministic calculator that validates each quadruple, excludes monotonicity violators, constructs the four cumulative response curves plus their complements, interpolates PMC/PME/IPP/OPP on the response-price union grid, and reports undefined crossings for degenerate data. T-VW-01…04 pin exclusion, a symmetric fixture, a hand-computed five-row fixture, and the undefined branch; §4.7 is now a required spec-coverage gate. Typed durable PSM records replace the former opaque slot while full JSON retains them and compact sharing still omits all research. Analyze gains nested Uncertainty/Research workbenches and an accessible PSM surface with manual/CSV paste, a demo dataset, validation report, SVG/table alternatives, acceptable-range shading, crossing cards, and a persistent illustrative mode that is explicitly labeled “SIMULATED — not evidence” with its model-to-prompt mapping disclosed. E2E-06 verifies demo points, exclusion count, undefined markers, and axe. Fresh local gates pass: Prettier, TypeScript, ESLint, 70 Vitest tests, spec coverage (six §4.7 citations), privacy scan (47 source files), static build, 281.9 KiB gzip client JS (≤300 KiB), `git diff --check`, and six Chromium flows with zero serious/critical axe findings. The sandbox denied the local Playwright server’s 127.0.0.1 bind, so the browser suite was rerun in the approved local environment. No files were staged, versioned, committed, pushed, or sent to remote CI/Vercel. P7b is ready for owner review; next implementation phase: P8 — Pricing Decision Record.

**S23 — 2026-07-25 (Codex, P7b commit series).** Owner approved an even, local-only three-commit P7b series at 15-minute intervals: `v0.8.4` Van Westendorp Engine + Survey State (`/1-F5-T6`, 00:15); `v0.8.5` Van Westendorp Research Workbench (`/2-F5-T6`, 00:30); and this `v0.8.6` Van Westendorp Acceptance + P7b Handoff (`/3-F5-T6`, 00:45). Each commit advances the patch version and aligns `package.json`, both root `package-lock.json` version fields, README's status/roadmap/Version Control block, and the footer through the package version. This final commit records the complete current-state handoff and P7b acceptance evidence. No push, remote CI, or deployment occurred. Next implementation phase: P8 — Pricing Decision Record.

**S24 — 2026-07-25 (Codex, P8 commit series).** Owner approved a local-only three-commit P8 series at 15-minute intervals: `v0.9.0` Decision Record Adapter + Extension Budget (`/1-F5-T6`, 01:00); `v0.9.1` Decision Record Surface + Share Foundation (`/2-F5-T6`, 01:15); and this `v0.9.2` P8 Shell Coverage + Handoff (`/3-F5-T6`, 01:30). The phase introduced no new runtime dependencies. `lib/state/decision-record.ts` composes assumptions + provenance, active design, simulated economics, sensitivity + validation to-dos, deterministic linter findings, alternatives, and optional Van Westendorp research into a Markdown artifact plus a print-CSS-friendly document; `components/decision-record-surface.tsx` renders the record, exposes Markdown download and Print/Save PDF, and gates generation behind an explicit “Generate current record” action; `components/share-surface.tsx` introduces Share sub-tabs (Decision Record, Scenario transfer), moves the JSON share/import/hash controls into a dedicated Scenario transfer surface, and reuses the shared `components/tab-keyboard.ts` roving-focus helper for horizontal tabs. `scripts/check-bundle-size.mjs` raises the client-JS gzip ceiling from 300 KiB to 320 KiB with an inline note explaining that the P7c/P7d-2 extensions push past the D-01 v1.0 ceiling; the P7d-2 handoff (§S29) records the ledger entry (D-34). Each commit aligns `package.json`, both root `package-lock.json` version fields, README's status/roadmap/Version Control block, and the footer through the package version. No push, remote CI, or deployment occurred. Next implementation phase: P8b — Pricing page mock.

**S25 — 2026-07-25 (Codex, P8b commit series).** Owner approved a local-only two-commit P8b series at 15-minute intervals: `v0.10.0` Pricing Page Surface + Share Wiring (`/1-F5-T6`, 01:45); and this `v0.10.1` P8b Shell Coverage + Handoff (`/2-F5-T6`, 02:00). `components/pricing-page-surface.tsx` renders the active design's tiers, prices, and included features as a theme-aware pricing-page mock that reuses the same source-of-truth active-design derivation as Simulate and Decision Record; `components/share-surface.tsx` adds the Pricing page sub-tab between Decision Record and Scenario transfer. The mock stays a presentation layer over the active design — no new pricing math. Each commit aligns `package.json`, both root `package-lock.json` version fields, README's status/roadmap/Version Control block, and the footer through the package version. No push, remote CI, or deployment occurred. Next implementation phase: P6b — Mechanism view + A/B compare.

**S26 — 2026-07-25 (Codex, P6b commit series).** Owner approved a local-only two-commit P6b series at 15-minute intervals: `v0.11.0` Mechanism View + Design Comparison Extensions (`/1-F5-T6`, 02:15); and this `v0.11.1` P6b Shell Coverage + Handoff (`/2-F5-T6`, 02:30). `components/simulation-extensions.tsx` exports two live surfaces: a Mechanism view rendering utility lines, indifference breakpoints, density strips, and a matching semantic table over the engine's envelope selection; and a Design comparison surface showing KPI deltas against the same buyer draws (common random numbers), win-rate framing, and a “promote to active” action. `components/wind-tunnel-surface.tsx` adds Mechanism and Compare designs sub-tabs alongside Overview and reuses the shared tab-keyboard helper. Every rendered breakpoint reconciles against the pure engine — the mechanism view is Wind Tunnel's expert-credibility surface and is engine-verified in the shell tests. Each commit aligns `package.json`, both root `package-lock.json` version fields, README's status/roadmap/Version Control block, and the footer through the package version. No push, remote CI, or deployment occurred. Next implementation phase: P7c — Bundling analyzer.

**S27 — 2026-07-25 (Codex, P7c commit series).** Owner approved a local-only three-commit P7c series at 15-minute intervals: `v0.12.0` Bundling Engine + Adams-Yellen Tests (`/1-F5-T6`, 02:45); `v0.12.1` Bundling Surface + Research Routing (`/2-F5-T6`, 03:00); and this `v0.12.2` P7c Acceptance + Handoff (`/3-F5-T6`, 03:15). `lib/engine/bundling.ts` reuses the shared upper-envelope engine for pure components, pure bundle, and mixed bundling regimes with a documented multi-start coordinate-ascent optimizer, explicit “not offered” sentinels, and honest reporting of the best price found under the searched grid. T-BND-01…04 pass, including the canonical σ→0 seller-favorable oracle. `components/bundling-surface.tsx` renders regime comparisons, disclosed prices searched, and a canonical teaching fixture; `components/research-surface.tsx` introduces the Analyze → Research nested tab shell routing PSM and Bundling, and `components/analyze-surface.tsx` swaps VanWestendorpSurface for the ResearchSurface wrapper so future extensions plug in without touching the Analyze tab. Each commit aligns `package.json`, both root `package-lock.json` version fields, README's status/roadmap/Version Control block, and the footer through the package version. No push, remote CI, or deployment occurred. Next implementation phase: P7d-1 — Conjoint + MaxDiff engines.

**S28 — 2026-07-25 (Codex, P7d-1 commit series).** Owner approved a local-only three-commit P7d-1 series at 15-minute intervals: `v0.13.0` Conjoint Engine + Derivative Gate (`/1-F5-T6`, 03:30); `v0.13.1` MaxDiff Engine + Balanced Design Generator (`/2-F5-T6`, 03:45); and this `v0.13.2` P7d-1 Handoff (`/3-F5-T6`, 04:00). `lib/engine/conjoint.ts` implements a pooled MNL log-likelihood with analytic gradient + analytic Hessian over log-sum-exp softmax utilities, damped Newton-Raphson to ‖∇ℓ‖∞ < 1e-8 or 50 iterations, explicit `ok` / `nonIdentifiable` / `separated` / `nonConverged` failure states, effects coding for non-price attributes, and a random-balanced design generator that rejects designs with duplicate concepts or rank-deficient designs. Standard errors are computed only from an invertible observed information matrix; the WTP-bridge proposal is enabled only when the numeric price coefficient's one-sided 90% upper bound stays strictly negative. The mandatory analytic-vs-finite-difference derivative gate (with `h = 1e-5·max(1, |β_i|)` and `absError ≤ 1e-7 + 1e-6·max(1, |finite|)`) precedes the T-CNJ-01 recovery test. `lib/engine/maxdiff.ts` implements best-worst counting scores, non-negative-sum-100 normalization with the total-item degenerate branch (uniform 100/k), and a balanced duplicate-free task-design generator. T-MXD-01…03 pin all-equal, non-negativity, and the named unseen-item validation error. No engines are wired into the shell yet — P7d-2 introduces the typed schemas, state adapters, and UI. Each commit aligns `package.json`, both root `package-lock.json` version fields, README's status/roadmap/Version Control block, and the footer through the package version. No push, remote CI, or deployment occurred. Next implementation phase: P7d-2 — Conjoint + MaxDiff UI + bridge.

**S29 — 2026-07-25 (Codex + Opus 4.8 review, P7d-2 commit series).** Owner approved a local-only P7d-2 series at 15-minute intervals, extended from four commits to five when an owner-directed cross-check found a shipped crash and an unmeetable acceptance gate: `v0.14.0` Typed Research Schemas + State Adapters (`/1-F5-T6`, 04:15); `v0.14.1` Decision Record Conjoint + MaxDiff Sections (`/2-F5-T6`, 04:30); `v0.14.2` Conjoint + MaxDiff Research Surfaces + Bridge (`/3-F5-T6`, 04:45); `v0.14.3` Conjoint Bridge Crash Fix + Shipped Synthetic Study (`/4-F5-T6`, 05:00); and this `v0.14.4` P7d-2 Acceptance + Handoff (`/5-F5-T6`, 05:15).

`lib/state/schemas.ts` replaces the opaque `conjoint` and `maxDiff` research slots with typed Zod schemas mirroring the engine contracts; `lib/state/conjoint.ts` and `lib/state/maxdiff.ts` add CSV parsers, durable study builders, and the pooled `applyConjointBridge(scenario, record, estimate, mapping)` action that translates significant-price attribute contrasts into per-feature account-value increments for a chosen segment, renormalizes allocations, and stamps `pooled conjoint (N=…)` provenance onto only the affected feature cells. `lib/state/decision-record.ts` is extended with optional Conjoint and MaxDiff sections; a `safelyEstimateConjoint` guard preserves record generation on non-identifiable studies. `components/conjoint-surface.tsx` provides the attribute/level definer, random-balanced design generator, response CSV entry, status heading, part-worth bar chart with 90% CI whiskers, hit-rate badge, and a segment/attribute→feature bridge gated on `bridgeEnabled`; `components/maxdiff-surface.tsx` provides the item list, design generator, response CSV entry, and normalized-importance bar chart; `components/research-surface.tsx` routes PSM → Bundling → Conjoint → MaxDiff.

**Two defects were found in `v0.14.2` after it was committed, both by running gates that the phase summary had claimed to run.** (1) `components/conjoint-surface.tsx` called `estimateConjointRecord(record)` in a render-path `useMemo` without guarding on `observations.length`; because "Generate task design" durably saves a study with zero observations, the very next render threw `RangeError: Conjoint estimation needs tasks and observed choices` and unmounted the surface — the mandatory first step of the workbench crashed it. Every other call site already guarded (`decision-record.ts` via `safelyEstimateConjoint`, `maxdiff-surface.tsx` via `responses.length > 0`); the surface was the sole unguarded consumer. Fixed in `v0.14.3` and pinned by the `studio-shell` acceptance test. (2) The only shipped conjoint fixture was a 4-row, 2-respondent `CONJOINT_DEMO_CSV`, which cannot identify an 8-parameter MNL — so **E2E-08 was unmeetable and P7d-2 could not honestly be called complete** (§0.6: never relabel incomplete work as complete). `v0.14.3` adds `buildSyntheticConjointStudy()`: a 3-attribute × 3-level CBC with numeric price and a "none" alternative, whose choices are sampled from `SYNTHETIC_CONJOINT_TRUE_BETA` under the same MNL the estimator fits. Both design and sampler are seeded, so the study is byte-identical on every load and is **generated rather than embedded** — it costs 0.5 KiB of bundle. At the shipped 120 respondents (2,160 observations) the estimator returns `ok`, recovers all eight generating coefficients within 3·SE, and reports a price coefficient of −0.031 against a true −0.03 with a 90% interval strictly below zero, which is what opens the bridge gate.

`v0.14.4` closes the phase's acceptance: the §7 L4 "part-worth-bar apply-bridge flow" test (previously absent) loads the synthetic study, asserts convergence and an open gate, maps Speed → Shared workspaces, applies the bridge, and verifies that **only** the mapped cell carries `pooled conjoint (N=120)` provenance while allocations stay normalized to 1. **E2E-08** runs the same flow in the browser and follows the provenance into the Decision Record's Markdown export, which is where the per-feature trace is expanded; a companion flow pins the honest disabled-bridge path on the unidentifiable demo CSV. Three pre-existing `e2e/shell.spec.ts` selectors were also repaired: `getByRole("tab", { name: "Design" })` had been ambiguous with P6b's "Compare designs" tab since `v0.11.0`, failing two flows under Playwright strict mode for eleven commits without detection because the e2e suite was never run (the P7d-2 summary reported it as blocked by the sandbox; it is not). Those selectors now pass `exact: true`.

**Ledger D-34** records the 300 → 320 KiB client-JS ceiling for v1.1 extension code; `scripts/check-bundle-size.mjs` had carried the raise since `v0.9.0`, but §3.5, P9's acceptance gate, and README still asserted 300 KiB — code and spec now agree. Measured footprint at phase close: **303.9 KiB / 320 KiB**. Verified at this commit: typecheck, lint, prettier, **111 unit tests**, build, and **10/10 Chromium e2e flows** including axe (0 serious/critical). Known-red history, not rewritten: `v0.9.1` fails one `studio-shell` import test because `v0.9.1` moved the JSON import controls behind the new Scenario transfer sub-tab while the test update was scheduled into `v0.9.2` — the same split-source-from-test shape that this session avoided by moving the PLG fixture probes into the same commit as the fixture correction. No push, remote CI, or deployment occurred. Next implementation phase: P9-partial — hardening + documentation.

**S30 — 2026-07-25 (Opus 4.8, P9-partial commit series).** Owner approved a local-only P9-partial series at 15-minute intervals, extended from three commits to four when the accessibility work found a real reflow defect: `v0.15.0` Contrast Fix + Template Corrections + Spec Coverage (`/1-F5-T6`, 05:30); `v0.15.1` CI + Deployment Config + Tri-Browser Matrix (`/2-F5-T6`, 05:45); `v0.15.2` Reflow Fix + Accessibility Evidence (`/3-F5-T6`, 06:00); and this `v0.15.3` Accessibility, Privacy, Release Documentation + Handoff (`/4-F5-T6`, 06:15). **This phase is P9-*partial*: it does not claim P9 or v1.0.0.** §6 P9 additionally requires the manual assistive-technology matrix, the owner's URL → decision-record user test, README screenshots, a live Vercel URL, and a MODEL-SPEC read-through — none of which are done. P0's remote CI and deployed-shell gates also remain open.

`v0.15.0` extends the visible focus ring from buttons/tabs to inputs, selects, textareas, and links, adds the skip-link and forced-colors styles, and corrects the PLG template's willingness-to-pay bands from per-seat to account level (Growing teams P50 25 → 250; Scaling organisations 80 → 1600). **The fixture correction and every test that reads it — `model-editing.test.ts`, `studio-shell.test.tsx`, and `e2e/shell.spec.ts` — land in this one commit.** The original plan scheduled the fixture apart from its dependent probes, which is exactly what left `v0.9.1` red; keeping them together is what keeps this series green at every step. `scripts/spec-coverage.mjs` additionally gates §4.6 (bundling), §4.9 (design linter), and **§4.10 (Conjoint/MaxDiff)** — §4.10 had 17 citations but was still labelled "future phase" after P7d-1/P7d-2 shipped. §4.11 stays optional until P7e exposes the positioning map.

`v0.15.1` turns on the tri-browser matrix §7 L4 requires at P9, and running it for the first time surfaced three defects the Chromium-only suite could not see. (1) `getByRole("tab", { name: "Design" })` matched both "Design" and P6b's "Compare designs" under Playwright strict mode — red since `v0.11.0`, repaired with `exact: true` in `v0.14.4`. (2) The reduced-motion assertion pinned a serialization (`0.01ms`) that Firefox spells `0.00001s`; it now compares parsed seconds. (3) The P6a/P7a budget flows call `newCDPSession`, which is Chromium-only, so they now declare `test.skip` on other engines rather than fail — an engine-specific performance gate, not a cross-browser behavioral contract.

`v0.15.2` fixes a genuine **WCAG 1.4.10** defect. `docs/ACCESSIBILITY-AUDIT.md` claimed automated coverage for the skip link, 24×24 pointer targets, 320 px reflow, and print media; **none of those tests existed**. Writing them found that the shell scrolled sideways by 206 px at 320 CSS px: Tailwind's `sr-only` is `position: absolute`, so inside the value matrix's `overflow-x-auto` region it had no positioned ancestor, resolved against the initial containing block, escaped the region's clip, and pinned the document to 526 px — while every visible box measured as correctly contained. Making scrollers `position: relative` contains the off-screen text without hiding it from assistive technology; `window.scrollX` at 320 px goes 206 → 0. The same commit makes axe wait for in-flight transitions, because a theme switch updates `--ink` on the root before the painted colours follow and WebKit's axe reliably sampled a pre-switch foreground against a post-switch background — a contrast failure no user ever sees.

Verified at this commit: typecheck, lint, prettier, spec-coverage, **111 unit tests**, build at **303.9 KiB / 320 KiB**, and the **full tri-browser Playwright matrix — 38 passed, 4 skipped (the two CDP budget flows on Firefox/WebKit), 0 failed, stable across three consecutive runs**, with zero serious/critical axe findings in either theme. `docs/ACCESSIBILITY-AUDIT.md` now separates automated evidence from the pending manual matrix and names both engine carve-outs; 200% text zoom, forced colors, price-sweep/tornado table parity, and screen-reader output are explicitly **not** claimed. The public claim stays **"WCAG 2.1 AA intent"** and must not move to "audited" until the manual matrix has a dated pass against a deployed release. `docs/PRIVACY.md`, `docs/RELEASE-CHECKLIST.md` (gzip gate corrected 300 → 320 KiB), and `docs/RELEASE-RUNBOOK.md` are added. Known-red history, not rewritten: `v0.9.1`. No push, remote CI, or deployment occurred. Next implementation phase: P7e — positioning (§6), or the remaining P9 manual gates for v1.0.0.

**S31 — 2026-07-26 (Opus 4.7, P7e — competitive positioning; committed as v0.16.0…v0.16.4).** Implemented the last §6 phase without adding runtime dependencies. `lib/engine/competitive.ts` is a pure, deterministic §4.11 module: `paretoFrontier` returns the discrete min-price-at-value staircase (with duplicate-coordinate collapse), `breakEvenRays` computes P10/P50/P90 ε slopes from the segment's within-segment σ, `directDominanceVerdict` applies the exact discrete predicate (V_c ≥ V_t and P_c ≤ P_t, strict in one), and `buildPositioningMap` composes them into the segment-scoped map. Competitor entry into the envelope engine keeps flowing through the existing `lib/engine/offers.ts` synthesis path from P1; the P7e engine is additive, not a second competitor pipeline. `lib/state/positioning.ts` adds `addCompetitor` / `renameCompetitor` / `setCompetitorPrice` / `setCompetitorPriceMetric` / `setCompetitorValueForSegment` / `setCompetitorOverallValue` / `removeCompetitor` (all preserving `scenarioSchema` validation) and `positioningMapForSegment`, which resolves each tier's account value against the active design and calls `buildPositioningMap`. `components/positioning-surface.tsx` renders competitor cards (name, price, flat/per-seat metric, per-segment value plus a one-shot "apply overall value" action), a segment selector, an SVG positioning map with P10/P50/P90 rays, a dashed staircase visually connecting Pareto points (never labeled a market offer), colored tier and competitor markers (directly-dominated tiers rendered in the amber Okabe–Ito accent), a matching "view as table" toggle, per-segment competitor share, and a scenario-level competitor-loss share readout. `components/analyze-surface.tsx` adds the Positioning sub-tab alongside Uncertainty and Research. `components/wind-tunnel-surface.tsx` replaces the P6a "Positioning phase" placeholder with the live competitor-loss share percentage (only visible once the simulator holds a competitor) and gives competitor offers a distinct dark-red dot swatch in the buyer-dot panel. `scripts/spec-coverage.mjs` now requires §4.11. Tests added: **T-CMP-01…07** in `lib/engine/competitive.test.ts` covering Pareto staircase construction with a Pareto point off the convex hull, break-even rays matching lognormal quantiles, duplicate-competitor collapse, direct-dominance predicate, real competitor share in the envelope with 5-term conservation, empty-competitor identity preservation to 1e-12, and a fast-check property over 0–5 competitors × sigma ∈ [0.1, 1.4] (T-CMP-08 already exists in `envelope.test.ts` from P1); `lib/state/positioning.test.ts` covers the four CRUD actions and the state-to-engine adapter; and one Positioning studio-shell integration test in `components/studio-shell.test.tsx` walks add → configure → simulate → observe a non-zero `competitorLossShare`. **E2E-09** in `e2e/shell.spec.ts` verifies the full flow: baseline "No competitors active" → Positioning workbench → add 3 competitors → configure one to dominate → chart renders (Chromium + Firefox + WebKit) → per-segment competitor share visible → return to Simulate → competitor-loss KPI live → dark-theme render + axe. Fresh local gates pass: Prettier, TypeScript, ESLint (including engine-purity), **124 unit tests** (up from 111 at v0.15.3), spec coverage (including 8 §4.11 citations), privacy scan (74 source files, unchanged), static build, and **307.8 KiB / 320 KiB gzip** client-JS (up 3.9 KiB from 303.9 KiB, well within the D-34 budget). Chromium ran all 15 flows including E2E-09; Firefox and WebKit ran E2E-09 green. No new ledger decision or dependency was introduced. Owner approved a five-commit local-only P7e series at 15-minute intervals, post-dated 2026-07-26 starting 00:15 +05:30, with suffix `/X-F5-O8`: `v0.16.0` Competitive Engine + Pareto Frontier Tests (`/1-F5-O8`, 00:15); `v0.16.1` Competitor State Adapter + CRUD Tests (`/2-F5-O8`, 00:30); `v0.16.2` Positioning Workbench + Analyze Routing (`/3-F5-O8`, 00:45); `v0.16.3` Live Competitor Loss KPI + Shell Coverage (`/4-F5-O8`, 01:00); and this `v0.16.4` P7e Acceptance + Handoff (`/5-F5-O8`, 01:15). Each commit advances the patch version and aligns `package.json`, both root `package-lock.json` versions, and README's Version Control block; the final commit adds the README status/roadmap edits, E2E-09, and this clean-pickup handoff. No push, remote CI, or deployment occurred. Next implementation phase: the remaining P9 manual gates for v1.0.0 (assistive-technology matrix, owner user-test, README screenshots) — the §6 build plan is otherwise complete.

**S32 — 2026-07-26 (Opus 4.8, v0.16.5 Sync State Push).** State-sync release, no functional or code change: bumped the version marker to `0.16.5` across `package.json`, both root `package-lock.json` fields, and README (status line + Base Format + Portfolio Version), and refreshed the handoff snapshot. One commit, `Sync State Push /O8`, post-dated 2026-07-26 01:30 +05:30. The later audit and implementation series supersedes this snapshot; its completed state is recorded in S34. No functional change, remote CI, or deployment.
